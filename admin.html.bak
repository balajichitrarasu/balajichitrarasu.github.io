<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
  <meta http-equiv="Pragma" content="no-cache">
  <meta http-equiv="Expires" content="0">
  <link rel="stylesheet" href="style.css?v=33.0">
  <script>
  var themeToggleLock = false;
  window.toggleTheme = function(e) {
    if (e) {
      if (typeof e.preventDefault === 'function') e.preventDefault();
      if (typeof e.stopPropagation === 'function') e.stopPropagation();
    }
    if (themeToggleLock) return;
    themeToggleLock = true;
    setTimeout(function() { themeToggleLock = false; }, 300);

    var cur = document.documentElement.getAttribute('data-theme') || 'dark';
    var next = cur === 'dark' ? 'light' : 'dark';

    document.documentElement.setAttribute('data-theme', next);
    if (document.body) document.body.setAttribute('data-theme', next);
    try { localStorage.setItem('site_theme', next); } catch(e) {}

    document.querySelectorAll('.glossy-theme-btn, #themeToggle').forEach(function(b) {
      b.setAttribute('title', next === 'dark' ? 'Switch to Light Mode' : 'Switch to Dark Mode');
    });

    if (typeof window.showToast === 'function') {
      window.showToast(next === 'dark' ? '🌙 Dark Mode Activated' : '☀️ Light Mode Activated');
    }
  };
  (function() {
    try {
      var saved = localStorage.getItem('site_theme') || 'dark';
      document.documentElement.setAttribute('data-theme', saved);
      if (document.body) document.body.setAttribute('data-theme', saved);
    } catch(e) {}
  })();
  </script>
  <style>
    /* Admin Specific CSS */
    .admin-container {
      display: flex;
      min-height: calc(100vh - var(--nav-h));
      padding-top: var(--nav-h);
    }

    /* ══ FUTURISTIC CYBER LOGIN & WELCOME ANIMATION STYLES ══ */
    .cyber-login-bg {
      position: fixed; inset: 0; z-index: 9999;
      background: radial-gradient(circle at 50% 30%, rgba(100, 255, 218, 0.08) 0%, rgba(15, 17, 23, 0.98) 70%);
      backdrop-filter: blur(28px);
      display: flex; align-items: center; justify-content: center; padding: 1.5rem;
      overflow: hidden;
    }
    .cyber-login-card {
      position: relative; z-index: 2;
      background: rgba(15, 20, 35, 0.85);
      border: 1px solid rgba(100, 255, 218, 0.25);
      border-radius: 20px; max-width: 450px; width: 100%;
      padding: 2.5rem 2.2rem; color: #fff;
      box-shadow: 0 30px 80px rgba(0, 0, 0, 0.9), 0 0 40px rgba(100, 255, 218, 0.12);
      backdrop-filter: blur(20px);
      transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    }
    .shield-badge-container {
      position: relative; width: 72px; height: 72px; margin: 0 auto 1.5rem;
      display: flex; align-items: center; justify-content: center;
    }
    .shield-ring {
      position: absolute; inset: -4px; border-radius: 50%;
      border: 2px dashed rgba(100, 255, 218, 0.5);
      animation: spinRing 12s linear infinite;
    }
    @keyframes spinRing { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
    .shield-icon-inner {
      width: 100%; height: 100%; border-radius: 50%;
      background: linear-gradient(135deg, rgba(100, 255, 218, 0.15), rgba(167, 139, 250, 0.15));
      border: 1px solid var(--teal); display: flex; align-items: center; justify-content: center;
      font-size: 2rem; box-shadow: 0 0 25px rgba(100, 255, 218, 0.3);
    }
    .form-input-wrapper {
      position: relative; margin-bottom: 1.2rem; text-align: left;
    }
    .form-input-wrapper input {
      width: 100%; padding: 0.85rem 1rem 0.85rem 2.6rem;
      background: rgba(255, 255, 255, 0.03); border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 10px; color: #fff; font-family: inherit; font-size: 0.9rem;
      transition: all 0.25s ease;
    }
    .form-input-wrapper input:focus {
      outline: none; background: rgba(100, 255, 218, 0.04);
      border-color: var(--teal); box-shadow: 0 0 15px rgba(100, 255, 218, 0.2);
    }
    .form-input-icon {
      position: absolute; left: 0.9rem; top: 2.3rem; color: var(--teal); font-size: 0.95rem; opacity: 0.8;
    }
    .welcome-overlay {
      position: fixed; inset: 0; z-index: 10000;
      background: rgba(8, 11, 20, 0.97); backdrop-filter: blur(30px);
      display: flex; flex-direction: column; align-items: center; justify-content: center;
      color: #fff; opacity: 0; pointer-events: none; transition: opacity 0.4s ease;
    }
    .welcome-overlay.show { opacity: 1; pointer-events: all; }
    .welcome-avatar {
      width: 80px; height: 80px; border-radius: 50%;
      background: linear-gradient(135deg, var(--teal), var(--accent));
      display: flex; align-items: center; justify-content: center;
      font-family: 'DM Mono', monospace; font-size: 1.8rem; font-weight: 800; color: #0f1117;
      box-shadow: 0 0 35px rgba(100, 255, 218, 0.5);
      animation: pulseAvatar 2s ease-in-out infinite; margin-bottom: 1.5rem;
    }
    @keyframes pulseAvatar { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.06); } }
    .welcome-bar-track {
      width: 280px; height: 4px; background: rgba(255, 255, 255, 0.1);
      border-radius: 99px; overflow: hidden; margin-top: 1.5rem;
    }
    .welcome-bar-fill {
      height: 100%; width: 0%; border-radius: 99px;
      background: linear-gradient(90deg, var(--teal), #a78bfa);
      transition: width 1.1s cubic-bezier(0.4, 0, 0.2, 1);
    }
    .admin-sidebar {
      width: 250px;
      background: var(--bg-card);
      border-right: 1px solid var(--border);
      padding: 2rem 1rem;
      position: fixed;
      height: calc(100vh - var(--nav-h));
      overflow-y: auto;
    }
    .admin-sidebar ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .admin-sidebar li {
      margin-bottom: 0.5rem;
    }
    .admin-sidebar a {
      display: block;
      padding: 0.75rem 1rem;
      color: var(--text-muted);
      text-decoration: none;
      border-radius: var(--radius-sm);
      transition: all var(--ease);
      cursor: pointer;
    }
    .admin-sidebar a:hover, .admin-sidebar a.active {
      background: rgba(100, 255, 218, 0.1);
      color: var(--teal);
    }
    .admin-content {
      flex: 1;
      margin-left: 250px;
      padding: 2rem;
      max-width: 1000px;
    }
    .tab-pane {
      display: none;
      animation: fadeIn var(--ease) forwards;
    }
    .tab-pane.active {
      display: block;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .form-group {
      margin-bottom: 1.5rem;
    }
    .form-group label {
      display: block;
      margin-bottom: 0.5rem;
      color: var(--text);
    }
    .form-control {
      width: 100%;
      padding: 0.75rem;
      background: var(--bg);
      border: 1px solid var(--border);
      border-radius: var(--radius-sm);
      color: var(--text);
      font-family: inherit;
    }
    .form-control:focus {
      outline: none;
      border-color: var(--teal);
    }
    .btn {
      padding: 0.75rem 1.5rem;
      background: var(--bg-card);
      color: var(--text);
      border: 1px solid var(--border);
      border-radius: var(--radius-sm);
      cursor: pointer;
      transition: all var(--ease);
    }
    .btn-primary {
      background: rgba(100, 255, 218, 0.1);
      color: var(--teal);
      border-color: var(--teal);
    }
    .btn-primary:hover {
      background: rgba(100, 255, 218, 0.2);
    }
    .btn-danger {
      background: rgba(244, 63, 94, 0.1);
      color: var(--rose);
      border-color: var(--rose);
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 1.5rem;
    }
    .card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 1.5rem;
    }
    .card-actions {
      display: flex;
      gap: 0.5rem;
      margin-top: 1rem;
    }
    /* Crop Tool */
    #crop-container {
      position: relative;
      display: inline-block;
      max-width: 500px;
      margin-top: 1rem;
    }
    #crop-canvas {
      border: 1px dashed var(--border);
      max-width: 100%;
      cursor: crosshair;
    }
    #crop-preview {
      margin-top: 1rem;
      max-width: 200px;
      border: 1px solid var(--border);
      border-radius: var(--radius-sm);
    }
    /* Toast */
    #toast {
      position: fixed;
      bottom: 2rem;
      right: 2rem;
      background: rgba(14, 19, 34, 0.95);
      border: 1px solid var(--teal);
      color: #fff;
      padding: 1rem 1.4rem;
      border-radius: 12px;
      transform: translateY(120px) scale(0.95);
      opacity: 0;
      transition: all 0.35s cubic-bezier(0.175, 0.885, 0.32, 1.275);
      z-index: 10000;
      box-shadow: 0 15px 40px rgba(0,0,0,0.8), 0 0 25px rgba(100,255,218,0.25);
      backdrop-filter: blur(16px);
      display: flex;
      align-items: center;
      gap: 0.8rem;
      max-width: 450px;
    }
    #toast.show {
      transform: translateY(0) scale(1);
      opacity: 1;
    }
    .toast-live-btn {
      display: inline-flex; align-items: center; gap: 0.3rem;
      padding: 0.35rem 0.75rem; background: var(--teal); color: #0f1117;
      font-weight: 700; font-size: 0.75rem; border-radius: 6px;
      text-decoration: none; transition: all 0.2s ease;
      white-space: nowrap; margin-left: 0.5rem;
    }
    .toast-live-btn:hover {
      transform: translateY(-2px); box-shadow: 0 0 12px rgba(100,255,218,0.6);
    }
    
    /* Prominent Inline Form Status Alerts */
    .status-alert {
      display: none;
      padding: 0.75rem 1rem;
      margin-top: 1rem;
      border-radius: var(--radius-sm);
      font-weight: 600;
      font-size: 0.88rem;
      animation: fadeIn 0.3s var(--ease);
      align-items: center;
      gap: 0.5rem;
    }
    .status-alert.success {
      display: flex;
      background: rgba(52, 211, 153, 0.12);
      border: 1px solid rgba(52, 211, 153, 0.4);
      color: var(--emerald);
    }
    .status-alert.error {
      display: flex;
      background: rgba(248, 113, 113, 0.12);
      border: 1px solid rgba(248, 113, 113, 0.4);
      color: var(--rose);
    }

    /* Tables */
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 1rem;
    }
    th, td {
      padding: 0.75rem;
      text-align: left;
      border-bottom: 1px solid var(--border);
    }
    th {
      color: var(--text-muted);
      font-weight: 500;
    }
    /* Glossy Day/Night Button */
    .glossy-theme-btn {
      display: inline-flex; align-items: center; gap: 0.4rem;
      padding: 0.4rem 0.95rem; border-radius: 99px;
      background: linear-gradient(135deg, rgba(255,255,255,0.12), rgba(255,255,255,0.03));
      border: 1px solid rgba(255,255,255,0.22);
      color: var(--teal); font-weight: 700; font-size: 0.78rem;
      cursor: pointer; backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
      box-shadow: 0 4px 15px rgba(0,0,0,0.2), inset 0 1px 1px rgba(255,255,255,0.3);
      transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
      margin-left: 0.4rem;
    }
    .glossy-theme-btn:hover {
      transform: translateY(-2px) scale(1.03);
      background: linear-gradient(135deg, rgba(100,255,218,0.25), rgba(167,139,250,0.25));
      border-color: var(--teal);
      box-shadow: 0 8px 25px rgba(100,255,218,0.3), inset 0 1px 2px rgba(255,255,255,0.5);
      color: #fff;
    }
    [data-theme="light"] .glossy-theme-btn {
      background: linear-gradient(135deg, rgba(13,148,136,0.12), rgba(13,148,136,0.04));
      border-color: rgba(13,148,136,0.3);
      color: #0d9488;
    }
    /* Cyber Input Groups with Embedded Icons & Working Pass Toggle */
    .cyber-input-group {
      position: relative;
      display: flex;
      align-items: center;
      margin-bottom: 1.2rem;
    }
    .cyber-input-icon {
      position: absolute;
      left: 1rem;
      font-size: 1.1rem;
      color: var(--teal);
      pointer-events: none;
      z-index: 2;
    }
    .cyber-input-field {
      width: 100%;
      padding: 0.85rem 1rem 0.85rem 2.8rem;
      background: rgba(15, 23, 42, 0.65);
      border: 1px solid rgba(255, 255, 255, 0.15);
      border-radius: 10px;
      color: #ffffff;
      font-family: 'Inter', sans-serif;
      font-size: 0.92rem;
      outline: none;
      transition: all 0.25s var(--ease);
    }
    .cyber-input-field:focus {
      border-color: var(--teal);
      background: rgba(15, 23, 42, 0.9);
      box-shadow: 0 0 0 3px rgba(100, 255, 218, 0.18), 0 0 20px rgba(100, 255, 218, 0.12);
    }
    .pass-toggle-btn {
      position: absolute;
      right: 0.6rem;
      top: 50%;
      transform: translateY(-50%);
      background: rgba(255, 255, 255, 0.08);
      border: 1px solid rgba(100, 255, 218, 0.3);
      color: var(--teal);
      font-size: 0.78rem;
      font-weight: 700;
      padding: 0.38rem 0.8rem;
      border-radius: 6px;
      cursor: pointer;
      transition: all 0.2s ease;
      z-index: 3;
    }
    .pass-toggle-btn:hover {
      background: var(--teal-dim);
      border-color: var(--teal);
      color: #ffffff;
      box-shadow: 0 0 10px rgba(100, 255, 218, 0.3);
    }
  </style>
</head>
<body>

  <nav class="navbar" id="navbar">
    <div class="nav-inner">
      <a class="nav-brand" href="index.html"><span class="nav-brand-badge">BC</span>Balaji Chitrarasu</a>
      <ul>
        <li><a href="index.html">← Portfolio</a></li>
        <li><a href="blog.html">Blog</a></li>
        <li><a href="admin.html" class="active nav-pill-v">⚙ Admin CMS</a></li>
        <li><button id="themeToggle" onclick="toggleTheme()" class="glossy-theme-btn" title="Toggle Day/Night Mode"><span class="theme-icon-dark">🌙</span><span class="theme-icon-light">☀️</span> Mode</button></li>
        <li><button id="nav-logout-btn" onclick="adminLogout()" class="btn" style="display:none; padding:0.3rem 0.8rem; font-size:0.78rem; background:rgba(244,63,94,0.12); color:var(--rose); border:1px solid rgba(244,63,94,0.3); border-radius:12px; margin-left:0.5rem; cursor:pointer;">🔒 Logout</button></li>
      </ul>
    </div>
  </nav>

  <!-- FUTURISTIC SECURE AUTHENTICATION GATE MODAL OVERLAY -->
  <div id="loginModal" class="cyber-login-bg">
    <div class="cyber-login-card">
      
      <div class="shield-badge-container">
        <div class="shield-ring"></div>
        <div class="shield-icon-inner">🛡️</div>
      </div>

      <h2 style="margin:0 0 0.4rem; font-size:1.55rem; color:var(--text); font-weight:800; letter-spacing:-0.02em;">Admin Security Portal</h2>
      <p style="margin:0 0 1.5rem; font-size:0.82rem; color:var(--text-muted);">Enter master credentials to access portfolio control system.</p>

      <div id="login-lockout-msg" style="display:none; background:rgba(244,63,94,0.12); border:1px solid rgba(244,63,94,0.3); color:var(--rose); padding:0.75rem; border-radius:8px; font-size:0.8rem; margin-bottom:1rem; text-align:left;"></div>
      <div id="login-error-msg" style="display:none; background:rgba(251,191,36,0.1); border:1px solid rgba(251,191,36,0.3); color:var(--amber); padding:0.75rem; border-radius:8px; font-size:0.8rem; margin-bottom:1rem; text-align:left;"></div>

      <form id="admin-login-form" action="javascript:void(0);" onsubmit="handleAdminLogin(event); return false;" autocomplete="off">
        <div style="text-align:left; margin-bottom:1rem;">
          <label style="display:block; font-size:0.8rem; font-weight:700; color:var(--text); margin-bottom:0.4rem;">Admin Username</label>
          <div class="cyber-input-group">
            <span class="cyber-input-icon">👤</span>
            <input type="text" id="login-username" class="cyber-input-field" placeholder="Enter admin username..." autocomplete="off" required>
          </div>
        </div>

        <div style="text-align:left; margin-bottom:1.2rem;">
          <label style="display:block; font-size:0.8rem; font-weight:700; color:var(--text); margin-bottom:0.4rem;">Master Password</label>
          <div class="cyber-input-group">
            <span class="cyber-input-icon">🔑</span>
            <input type="password" id="login-password" class="cyber-input-field" style="padding-right: 5.5rem;" placeholder="Enter master password..." autocomplete="off" required>
            <button type="button" id="toggle-pass-btn" onclick="togglePasswordVisibility(event)" class="pass-toggle-btn">👁️ Show</button>
          </div>
        </div>

        <div style="display:flex; align-items:center; justify-content:space-between; margin-bottom:1.5rem; font-size:0.8rem; color:var(--text-muted);">
          <label style="display:flex; align-items:center; gap:0.5rem; cursor:pointer; user-select:none;">
            <input type="checkbox" id="login-remember" checked style="width:16px; height:16px; accent-color:var(--teal); cursor:pointer;"> Remember Session (7 Days)
          </label>
          <span style="color:var(--teal); font-size:0.72rem; font-weight:700; font-family:'DM Mono',monospace; background:rgba(100,255,218,0.08); padding:0.2rem 0.65rem; border-radius:12px; border:1px solid rgba(100,255,218,0.25);">🔒 SHA-256 Auth</span>
        </div>

        <button type="submit" onclick="handleAdminLogin(event)" id="login-submit-btn" class="btn btn-primary" style="width:100%; padding:0.95rem; font-size:0.98rem; font-weight:700; border-radius:10px; cursor:pointer; background:linear-gradient(135deg, rgba(100,255,218,0.25), rgba(167,139,250,0.25)); border:1px solid var(--teal); color:#ffffff; box-shadow:0 8px 25px rgba(100,255,218,0.2); transition:all 0.25s var(--ease);">
          🔓 Authenticate &amp; Enter CMS
        </button>
      </form>
    </div>
  </div>

  <!-- HIGH-TECH WELCOME ANIMATION OVERLAY -->
  <div id="welcomeOverlay" class="welcome-overlay">
    <div class="welcome-avatar">BC</div>
    <h2 style="font-size:1.6rem; font-weight:800; color:#fff; margin:0 0 0.5rem; text-shadow:0 0 20px rgba(100,255,218,0.4);">🔓 Welcome Back, Admin!</h2>
    <p id="welcomeStatusText" style="font-size:0.85rem; color:var(--teal); margin:0; font-family:'DM Mono',monospace;">⚡ Verifying Security Signatures...</p>
    <div class="welcome-bar-track">
      <div id="welcomeBarFill" class="welcome-bar-fill"></div>
    </div>
  </div>

  <div class="admin-container" style="display:none;">
    <aside class="admin-sidebar">
      <ul>
        <li><a class="tab-link active" data-tab="analytics">📊 Analytics</a></li>
        <li><a class="tab-link" data-tab="profile">👤 Profile, Bio &amp; Resume</a></li>
        <li><a class="tab-link" data-tab="experience">💼 Experience &amp; Timeline</a></li>
        <li><a class="tab-link" data-tab="certs">🏆 Certifications &amp; Docs</a></li>
        <li><a class="tab-link" data-tab="projects">💻 Projects Manager</a></li>
        <li><a class="tab-link" data-tab="skills">🎯 Skills &amp; Competencies</a></li>
        <li><a class="tab-link" data-tab="ecosystem">🌐 Tooling Ticker</a></li>
        <li><a class="tab-link" data-tab="events">🎉 Events &amp; Showcase</a></li>
        <li><a class="tab-link" data-tab="blog">📝 Blog Manager</a></li>
        <li><a class="tab-link" data-tab="database">💾 Database &amp; Backup</a></li>
        <li><a class="tab-link" data-tab="settings">⚙ Settings</a></li>
        <li><a class="tab-link" data-tab="logs">🛡️ Access &amp; IP Logs</a></li>
        <li style="margin-top:1.5rem; border-top:1px solid var(--border); padding-top:0.75rem;"><a id="sidebar-logout-btn" onclick="adminLogout()" style="color:var(--rose); cursor:pointer;">🔒 Logout Session</a></li>
      </ul>
    </aside>

    <main class="admin-content">
      
      <!-- Analytics Tab -->
      <section id="analytics" class="tab-pane active">
        <h2>Analytics Overview</h2>
        <div class="grid">
          <div class="card">
            <h3>Page Views</h3>
            <p id="stat-views" style="font-size: 2rem; color: var(--teal);">1,280</p>
          </div>
          <div class="card">
            <h3>Projects</h3>
            <p id="stat-projects" style="font-size: 2rem; color: var(--violet);">3</p>
          </div>
          <div class="card">
            <h3>Certifications</h3>
            <p id="stat-certs" style="font-size: 2rem; color: var(--emerald);">4</p>
          </div>
          <div class="card">
            <h3>Blog Posts</h3>
            <p id="stat-blogs" style="font-size: 2rem; color: var(--amber);">2</p>
          </div>
        </div>
        <div class="card" style="margin-top: 1.5rem;">
          <h3>System Status</h3>
          <p>Status: <span style="color: var(--teal)">Healthy</span></p>
          <p>Last Visit: <span id="stat-last-visit">-</span></p>
        </div>
      </section>

      <!-- Profile Tab -->
      <section id="profile" class="tab-pane">
        <h2 style="color:var(--text); font-weight:700; margin-bottom:1.5rem;">Profile &amp; Avatar</h2>
        <div class="card" style="background:rgba(255,255,255,0.025); border:1px solid var(--border); border-radius:10px; padding:1.8rem;">
          <form id="profile-form" action="javascript:void(0);" onsubmit="event.preventDefault(); return false;" autocomplete="off">
            <div class="form-group">
              <label style="font-weight:600;">Avatar Image File Upload &amp; Cropper</label>
              <input type="file" id="prof-avatar-file" class="form-control" accept="image/*">
              <input type="text" id="prof-avatar" class="form-control" placeholder="Or enter Avatar Image URL / Base64" style="margin-top: 0.5rem;">
              <div style="margin-top: 1rem; display: flex; align-items: center; gap: 1rem; flex-wrap:wrap;">
                <img id="prof-avatar-preview" src="profile.jpg" style="width: 80px; height: 80px; border-radius: 50%; object-fit: cover; border: 2px solid var(--teal); background: rgba(255,255,255,0.05);">
                <button type="button" class="btn btn-primary" onclick="openCropperForTarget('prof-avatar', 'prof-avatar-preview', 1)" style="font-size:0.8rem; padding:0.45rem 1rem;">✂️ Crop Avatar Image</button>
                <button type="button" class="btn" onclick="exportProfileJpg()" style="font-size:0.8rem; padding:0.45rem 1rem;">📥 Download as profile.jpg for Repo</button>
              </div>
              <p style="font-size:0.75rem; color:var(--text-muted); margin-top:0.5rem; line-height:1.4;">
                💡 <strong>Cross-Device Reflection Note:</strong> LocalStorage avatar saves to your current browser. To make your new photo reflect permanently on <strong>ALL mobile phones &amp; secondary devices</strong>, click <em>"Download as profile.jpg"</em> and replace <code>G:\Portfolio\profile.jpg</code>!
              </p>
            </div>
            <div class="form-group">
              <label style="font-weight:600;">Display Name</label>
              <input type="text" id="prof-name" class="form-control" placeholder="Balaji Chitrarasu">
            </div>
            <div class="form-group">
              <label style="font-weight:600;">Role &amp; Tagline</label>
              <input type="text" id="prof-role" class="form-control" placeholder="AWS Cloud Engineer Intern & B.E. ECE Student">
            </div>
            <div class="form-group">
              <label style="font-weight:600;">Location</label>
              <input type="text" id="prof-location" class="form-control" placeholder="Coimbatore, Tamil Nadu, India">
            </div>

            <!-- RESUME PDF & DOCUMENT UPLOADER -->
            <div class="form-group" style="border-top:1px solid var(--border); padding-top:1.2rem; margin-top:1.2rem;">
              <label style="font-weight:700; color:var(--teal);">📄 Resume PDF / Document Upload</label>
              <p style="font-size:0.78rem; color:var(--text-muted); margin-bottom:0.5rem;">Upload your Resume PDF file or enter URL. All "Resume" download buttons on the portfolio site will link to this file!</p>
              <input type="file" id="prof-resume-file" class="form-control" accept=".pdf,.doc,.docx,image/*">
              <input type="text" id="prof-resume-url" class="form-control" placeholder="Or enter Resume PDF URL (e.g. Resume.pdf or Google Drive link)" style="margin-top:0.5rem;">
              <div id="resume-preview-status" style="margin-top:0.5rem; display:none; padding:0.6rem; background:rgba(52,211,153,0.1); border:1px solid rgba(52,211,153,0.3); border-radius:6px; color:var(--emerald); font-weight:600; font-size:0.8rem;">
                📄 Resume PDF Document Ready! <a id="resume-preview-link" href="#" target="_blank" style="color:var(--teal); margin-left:0.5rem; text-decoration:underline;">View Attached Resume →</a>
              </div>
            </div>

            <div id="profile-alert" class="status-alert" style="display:none;"></div>
            <button type="submit" id="profile-submit-btn" class="btn btn-primary" style="margin-top:0.5rem;">💾 Save Profile &amp; Resume PDF</button>
          </form>
        </div>
      </section>

      <!-- Experience & Timeline Tab -->
      <section id="experience" class="tab-pane">
        <h2 style="color:var(--text); font-weight:700; margin-bottom:1.5rem;">💼 Experience &amp; Education Manager</h2>
        <div class="card" style="margin-bottom: 2rem; background:rgba(255,255,255,0.025); border:1px solid var(--border); border-radius:10px; padding:1.8rem;">
          <h3 id="exp-form-title" style="margin-top:0; color:var(--teal);">Add Timeline Entry</h3>
          <form id="exp-form" action="javascript:void(0);" onsubmit="event.preventDefault(); saveExperience(); return false;" autocomplete="off">
            <input type="hidden" id="exp-id">
            <div class="form-group">
              <label style="font-weight:600;">Role or Degree Title *</label>
              <input type="text" id="exp-role" class="form-control" placeholder="e.g. AWS Cloud Engineer Intern or B.E. ECE" required>
            </div>
            <div class="form-group">
              <label style="font-weight:600;">Organization / Institution *</label>
              <input type="text" id="exp-org" class="form-control" placeholder="e.g. ZenFuture Technologies or United Institute of Technology" required>
            </div>
            <div class="form-group">
              <label style="font-weight:600;">Date Range / Duration *</label>
              <input type="text" id="exp-date" class="form-control" placeholder="e.g. JUNE 2026 — JULY 2026 or 2023 — 2027" required>
            </div>
            <div class="form-group">
              <label style="font-weight:600;">Description &amp; Key Highlights</label>
              <textarea id="exp-desc" class="form-control" rows="3" placeholder="Describe key responsibilities, AWS services deployed, achievements, CGPA, etc."></textarea>
            </div>
            <div id="exp-alert" class="status-alert" style="display:none;"></div>
            <button type="submit" id="exp-submit-btn" class="btn btn-primary">💾 Save Timeline Entry</button>
            <button type="button" class="btn" id="exp-cancel" onclick="clearExpForm()" style="display:none; margin-left:0.5rem;">Cancel</button>
          </form>
        </div>
        <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1rem;">
          <h3 style="margin:0;">Existing Timeline Entries</h3>
          <button type="button" class="btn btn-primary" onclick="restoreTimeline()" style="padding:0.3rem 0.8rem; font-size:0.8rem;">↻ Restore Default Timeline</button>
        </div>
        <div id="exp-list" class="grid"></div>
      </section>

      <!-- Certs Tab -->
      <section id="certs" class="tab-pane">
        <h2>Certifications Manager</h2>
        <div class="card" style="margin-bottom: 2rem;">
          <h3 id="cert-form-title">Add Certification</h3>
          <form id="cert-form" action="javascript:void(0);" onsubmit="event.preventDefault(); return false;" autocomplete="off">
            <input type="hidden" id="cert-id">
            <div class="form-group">
              <label>Title *</label>
              <input type="text" id="cert-title" class="form-control" required>
            </div>
            <div class="form-group">
              <label>Issuing Organization *</label>
              <input type="text" id="cert-org" class="form-control" required>
            </div>
            <div class="form-group">
              <label>Status / Date</label>
              <input type="text" id="cert-date" class="form-control">
            </div>
            <div class="form-group">
              <label>Description</label>
              <textarea id="cert-desc" class="form-control" rows="3"></textarea>
            </div>
            <div class="form-group">
              <label>Certificate Image or PDF Document Upload</label>
              <input type="file" id="cert-file" class="form-control" accept="image/*,.pdf">
              <input type="text" id="cert-image" class="form-control" placeholder="Or enter Image / PDF URL directly (e.g. cert1.jpg or document.pdf)" style="margin-top: 0.5rem;">
              <button type="button" class="btn btn-primary" onclick="openCropperForTarget('cert-image', 'crop-preview', 1.777)" style="margin-top:0.5rem; font-size:0.8rem; padding:0.35rem 0.8rem;">✂️ Crop Certificate Image</button>
              
              <div style="margin-top:0.5rem;">
                <img id="crop-preview" style="display:none; max-width:100%; max-height:160px; border-radius:6px; border:1px solid var(--border);">
                <div id="pdf-preview" style="display:none; padding:0.8rem; background:rgba(255,255,255,0.03); border:1px solid var(--border); border-radius:6px; color:var(--teal); font-weight:600; font-size:0.85rem;">📄 PDF Certificate Attached</div>
              </div>
            </div>
            <button type="submit" class="btn btn-primary">Save Certification</button>
            <button type="button" class="btn" id="cert-cancel" style="display:none;">Cancel</button>
          </form>
        </div>
        <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1rem;">
          <h3 style="margin:0;">Existing Certifications</h3>
          <button type="button" class="btn btn-primary" onclick="restoreCerts()" style="padding:0.3rem 0.8rem; font-size:0.8rem;">↻ Restore Default Certifications</button>
        </div>
        <div id="cert-list" class="grid"></div>
      </section>

      <!-- Projects Tab -->
      <section id="projects" class="tab-pane">
        <h2>Projects Manager</h2>
        <div class="card" style="margin-bottom: 2rem;">
          <h3 id="proj-form-title">Add Project</h3>
          <form id="proj-form" action="javascript:void(0);" onsubmit="event.preventDefault(); return false;" autocomplete="off">
            <input type="hidden" id="proj-id">
            <div class="form-group">
              <label>Title *</label>
              <input type="text" id="proj-title" class="form-control" required>
            </div>
            <div class="form-group">
              <label>Category</label>
              <select id="proj-cat" class="form-control">
                <option value="cloud">Cloud</option>
                <option value="embedded">Embedded</option>
                <option value="web">Web</option>
                <option value="other">Other</option>
              </select>
            </div>
            <div class="form-group">
              <label>Cover Image File Upload &amp; Cropper</label>
              <input type="file" id="proj-file" class="form-control" accept="image/*">
              <input type="text" id="proj-image" class="form-control" placeholder="Or enter Cover Image URL directly" style="margin-top:0.5rem;">
              <button type="button" class="btn btn-primary" onclick="openCropperForTarget('proj-image', null, 1.777)" style="margin-top:0.5rem; font-size:0.8rem; padding:0.35rem 0.8rem;">✂️ Crop Cover Image</button>
            </div>
            <div class="form-group">
              <label>Multiple Gallery Images (Comma separated URLs or Base64)</label>
              <textarea id="proj-gallery" class="form-control" rows="2" placeholder="e.g. project1.jpg, project2.jpg, project3.jpg"></textarea>
            </div>
            <div class="form-group">
              <label>Tech Tags (comma separated)</label>
              <input type="text" id="proj-tags" class="form-control">
            </div>
            <div class="form-group">
              <label>Short Summary</label>
              <textarea id="proj-summary" class="form-control" rows="2"></textarea>
            </div>
            <div class="form-group">
              <label>Full Details</label>
              <textarea id="proj-details" class="form-control" rows="4"></textarea>
            </div>
            <div class="form-group">
              <label>Status</label>
              <select id="projStatus" class="form-control">
                <option value="completed">Completed</option>
                <option value="ongoing" selected>Ongoing</option>
                <option value="planned">Planned</option>
              </select>
            </div>
            <div class="form-group">
              <label>Progress Percentage (<span id="projProgressVal">65</span>%)</label>
              <input type="range" id="projProgress" class="form-control" min="0" max="100" value="65">
            </div>
            <div class="form-group">
              <label>Live Demo URL</label>
              <input type="text" id="projDemo" class="form-control">
            </div>
            <div class="form-group">
              <label>GitHub Repo URL</label>
              <input type="text" id="projRepo" class="form-control">
            </div>
            <script>
              document.getElementById('projStatus').addEventListener('change', function(e) {
                const val = e.target.value;
                const prog = document.getElementById('projProgress');
                if (val === 'completed') prog.value = 100;
                if (val === 'planned') prog.value = 0;
                document.getElementById('projProgressVal').innerText = prog.value;
              });
              document.getElementById('projProgress').addEventListener('input', function(e) {
                document.getElementById('projProgressVal').innerText = e.target.value;
              });
            </script>
            <button type="submit" class="btn btn-primary">Save Project</button>
            <button type="button" class="btn" id="proj-cancel" style="display:none;">Cancel</button>
          </form>
        </div>
        <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1rem;">
          <h3 style="margin:0;">Existing Projects</h3>
          <button type="button" class="btn btn-primary" onclick="restoreProjects()" style="padding:0.3rem 0.8rem; font-size:0.8rem;">↻ Restore Default Projects</button>
        </div>
        <div id="proj-list" class="grid"></div>
      </section>

      <!-- Skills Tab -->
      <section id="skills" class="tab-pane">
        <h2>Skills Bar Editor</h2>
        <div class="card" style="margin-bottom: 2rem;">
          <form id="skill-form" action="javascript:void(0);" onsubmit="event.preventDefault(); return false;" autocomplete="off">
            <input type="hidden" id="skill-index">
            <div class="flex-row" style="flex-wrap: wrap;">
              <div class="form-group" style="flex:1;">
                <label>Name</label>
                <input type="text" id="skill-name" class="form-control" required>
              </div>
              <div class="form-group" style="width: 100px;">
                <label>Icon</label>
                <input type="text" id="skill-icon" class="form-control">
              </div>
              <div class="form-group" style="width: 150px;">
                <label>Percentage (0-100)</label>
                <input type="number" id="skill-pct" class="form-control" min="0" max="100" required>
              </div>
              <div class="form-group" style="flex:1;">
                <label>Category</label>
                <input type="text" id="skill-cat" class="form-control">
              </div>
            </div>
            <button type="submit" class="btn btn-primary">Save Skill</button>
            <button type="button" class="btn" id="skill-cancel" style="display:none;">Cancel</button>
            <button type="button" class="btn btn-danger" id="skill-restore" style="float: right;">Restore Defaults</button>
          </form>
        </div>
        <div class="card">
          <table>
            <thead>
              <tr>
                <th>Order</th>
                <th>Icon</th>
                <th>Name</th>
                <th>Category</th>
                <th>Pct</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody id="skill-tbody"></tbody>
          </table>
        </div>
      </section>

      <!-- Tooling Ticker & Ecosystem Tab -->
      <section id="ecosystem" class="tab-pane">
        <h2 style="color:var(--text); font-weight:700; margin-bottom:1.5rem;">🌐 Tooling Ticker &amp; Ecosystem Manager</h2>
        <div class="card" style="margin-bottom: 2rem; background:rgba(255,255,255,0.025); border:1px solid var(--border); border-radius:10px; padding:1.8rem;">
          <h3 id="ticker-form-title" style="margin-top:0; color:var(--teal);">Add Tooling Ticker Item</h3>
          <form id="ticker-form" action="javascript:void(0);" onsubmit="event.preventDefault(); saveTickerItem(); return false;" autocomplete="off">
            <input type="hidden" id="ticker-id">
            <div class="form-group">
              <label style="font-weight:600;">Technology / Tool Name *</label>
              <input type="text" id="ticker-name" class="form-control" placeholder="e.g. AWS Cloud, Docker, Postman, Linux Bash" required>
            </div>
            <div class="form-group">
              <label style="font-weight:600;">Icon (SimpleIcons slug, SVG Data URI, or Image URL)</label>
              <input type="text" id="ticker-icon" class="form-control" placeholder="e.g. python, docker, linux, aws, or image URL">
              <span style="font-size:0.75rem; color:var(--text-muted); display:block; margin-top:0.3rem;">For standard tech icons, enter slugs like: <code>python</code>, <code>docker</code>, <code>mysql</code>, <code>linux</code>, <code>git</code>, <code>github</code>, <code>postman</code>. AWS uses an auto-embedded SVG URI.</span>
            </div>
            <div id="ticker-alert" class="status-alert" style="display:none;"></div>
            <button type="submit" id="ticker-submit-btn" class="btn btn-primary">💾 Save Ticker Item</button>
            <button type="button" class="btn" id="ticker-cancel" onclick="clearTickerForm()" style="display:none; margin-left:0.5rem;">Cancel</button>
          </form>
        </div>
        <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1rem;">
          <h3 style="margin:0;">Current Tooling Ticker Ecosystem</h3>
          <button type="button" class="btn btn-primary" onclick="restoreTicker()" style="padding:0.3rem 0.8rem; font-size:0.8rem;">↻ Restore Default Ticker</button>
        </div>
        <div id="ticker-list" class="grid"></div>
      </section>

      <!-- Events & Leadership Showcase Tab -->
      <section id="events" class="tab-pane">
        <h2 style="color:var(--text); font-weight:700; margin-bottom:1.5rem;">🎉 Events &amp; Leadership Showcase Manager</h2>
        <div class="card" style="margin-bottom: 2rem; background:rgba(255,255,255,0.025); border:1px solid var(--border); border-radius:10px; padding:1.8rem;">
          <h3 id="events-form-title" style="margin-top:0; color:var(--teal);">Add Event / Award Item</h3>
          <form id="events-form" action="javascript:void(0);" onsubmit="event.preventDefault(); saveEventItem(); return false;" autocomplete="off">
            <input type="hidden" id="events-id">
            <div class="form-group">
              <label style="font-weight:600;">Event / Award Title *</label>
              <input type="text" id="events-title" class="form-control" placeholder="e.g. Exemplary Achiever Award or Production AWS Setup" required>
            </div>
            <div class="form-group">
              <label style="font-weight:600;">Subtitle / Category *</label>
              <input type="text" id="events-org" class="form-control" placeholder="e.g. Academic Excellence — ECE Department" required>
            </div>
            <div class="form-group">
              <label style="font-weight:600;">Location / Organization *</label>
              <input type="text" id="events-location" class="form-control" placeholder="e.g. United Institute of Technology, Coimbatore" required>
            </div>
            <div class="form-group">
              <label style="font-weight:600;">Description</label>
              <textarea id="events-desc" class="form-control" rows="3" placeholder="Describe award details, recognition, AICTE / Ministry of Education roles, etc."></textarea>
            </div>
            <div class="form-group" style="display:flex; gap:1rem;">
              <div style="flex:1;">
                <label style="font-weight:600;">Icon Emoji / Symbol</label>
                <input type="text" id="events-icon" class="form-control" placeholder="e.g. 🎖️, ☁️, 💡, 🏆">
              </div>
              <div style="flex:1;">
                <label style="font-weight:600;">Badge Color Accent</label>
                <select id="events-color" class="form-control">
                  <option value="gold">Gold 🎖️</option>
                  <option value="teal">Teal ☁️</option>
                  <option value="violet">Violet 💡</option>
                </select>
              </div>
            </div>
            <div id="events-alert" class="status-alert" style="display:none;"></div>
            <button type="submit" id="events-submit-btn" class="btn btn-primary">💾 Save Event / Award</button>
            <button type="button" class="btn" id="events-cancel" onclick="clearEventsForm()" style="display:none; margin-left:0.5rem;">Cancel</button>
          </form>
        </div>
        <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1rem;">
          <h3 style="margin:0;">Current Events &amp; Awards</h3>
          <button type="button" class="btn btn-primary" onclick="restoreEvents()" style="padding:0.3rem 0.8rem; font-size:0.8rem;">↻ Restore Default Events</button>
        </div>
        <div id="events-list" class="grid"></div>
      </section>

      <!-- Blog Tab -->
      <section id="blog" class="tab-pane">
        <h2>Blog Manager</h2>
        <div class="card" style="margin-bottom: 2rem;">
          <h3 id="blog-form-title">Write Post</h3>
          <form id="blog-form" action="javascript:void(0);" onsubmit="event.preventDefault(); return false;" autocomplete="off">
            <input type="hidden" id="blog-id">
            <div class="form-group">
              <label>Title *</label>
              <input type="text" id="blog-title" class="form-control" required>
            </div>
            <div class="form-group">
              <label>Category</label>
              <input type="text" id="blog-cat" class="form-control">
            </div>
            <div class="form-group">
              <label>Excerpt</label>
              <textarea id="blog-excerpt" class="form-control" rows="2"></textarea>
            </div>
            <div class="form-group">
              <label>Cover / Header Image Upload &amp; Cropper</label>
              <input type="file" id="blog-image-file" class="form-control" accept="image/*">
              <input type="text" id="blog-image" class="form-control" placeholder="Or enter Image URL directly" style="margin-top: 0.5rem;">
              <button type="button" class="btn btn-primary" onclick="openCropperForTarget('blog-image', null, 1.777)" style="margin-top:0.5rem; font-size:0.8rem; padding:0.35rem 0.8rem;">✂️ Crop Blog Cover Image</button>
            </div>
            <div class="form-group">
              <label>Attach PDF / Technical Report / Document (PDF / DOCX)</label>
              <input type="file" id="blog-pdf-file" class="form-control" accept="application/pdf,.doc,.docx">
              <input type="text" id="blog-pdf" class="form-control" placeholder="Or enter Document PDF URL directly" style="margin-top: 0.5rem;">
              <div id="blog-pdf-status" style="margin-top:0.3rem; font-size:0.78rem; color:var(--teal);"></div>
            </div>
            <div class="form-group">
              <label>Report Diagrams &amp; Screenshots Gallery (Comma separated URLs)</label>
              <textarea id="blog-gallery" class="form-control" rows="2" placeholder="e.g. diagram1.png, chart2.png"></textarea>
            </div>
            <div class="form-group">
              <label>Body</label>
              <textarea id="blog-body" class="form-control" rows="8"></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Publish Post</button>
            <button type="button" class="btn" id="blog-cancel" style="display:none;">Cancel</button>
          </form>
        </div>
        <h3>Existing Posts</h3>
        <div id="blog-list" class="grid"></div>
      </section>

      <!-- Database Tab -->
      <section id="database" class="tab-pane">
        <h2>Database Backup</h2>
        <div class="card">
          <p>Export all your local storage data as a JSON file, or import an existing one.</p>
          <div style="margin-top: 1rem; display: flex; gap: 1rem;">
            <button id="db-export" class="btn btn-primary">Export JSON</button>
            <label class="btn">
              Import JSON
              <input type="file" id="db-import" accept=".json" style="display:none;">
            </label>
            <button id="db-reset" class="btn btn-danger">Reset All Data</button>
          </div>
          <div style="margin-top: 2rem;">
            <label>Raw Storage Content:</label>
            <textarea id="db-preview" class="form-control" rows="10" readonly></textarea>
          </div>
        </div>
      </section>

      <!-- Settings Tab -->
      <section id="settings" class="tab-pane">
        <h2>Site Settings</h2>
        <div class="card">
          <form id="settings-form" action="javascript:void(0);" onsubmit="event.preventDefault(); return false;" autocomplete="off">
            <div class="form-group">
              <label>Email</label>
              <input type="email" id="set-email" class="form-control">
            </div>
            <div class="form-group">
              <label>Phone</label>
              <input type="text" id="set-phone" class="form-control">
            </div>
            <div class="form-group">
              <label>GitHub URL</label>
              <input type="url" id="set-github" class="form-control">
            </div>
            <div class="form-group">
              <label>LinkedIn URL</label>
              <input type="url" id="set-linkedin" class="form-control">
            </div>
            <div class="form-group">
              <label>Upload New Resume Document (PDF / DOCX)</label>
              <input type="file" id="set-resume-file" class="form-control" accept="application/pdf,.doc,.docx">
              <input type="text" id="set-resume" class="form-control" placeholder="Or enter Resume URL directly (e.g. resume.pdf)" style="margin-top: 0.5rem;">
              <div id="resume-status" style="margin-top:0.4rem; font-size:0.78rem; color:var(--teal);"></div>
            </div>
            <hr style="border-color:var(--border); margin:1.5rem 0;">
            <h3 style="margin-bottom:0.4rem; color:var(--teal); font-weight:700;">🎛️ Section Visibility Control (Toggle ON/OFF)</h3>
            <p style="font-size:0.8rem; color:var(--text-muted); margin-bottom:1.2rem;">Enable or disable visibility of any section on your live portfolio homepage instantly with slick ON/OFF toggle switches.</p>

            <div class="toggle-switch-grid">
              <div class="toggle-switch-group">
                <span class="toggle-label-text">⚡ Availability Banner</span>
                <label class="switch-control">
                  <input type="checkbox" id="toggle-avail" checked onchange="updateToggleBadge(this)">
                  <span class="switch-slider"></span>
                </label>
              </div>

              <div class="toggle-switch-group">
                <span class="toggle-label-text">📊 Live Stats Counters</span>
                <label class="switch-control">
                  <input type="checkbox" id="toggle-stats" checked onchange="updateToggleBadge(this)">
                  <span class="switch-slider"></span>
                </label>
              </div>

              <div class="toggle-switch-group">
                <span class="toggle-label-text">👤 Background &amp; Core Bio</span>
                <label class="switch-control">
                  <input type="checkbox" id="toggle-about" checked onchange="updateToggleBadge(this)">
                  <span class="switch-slider"></span>
                </label>
              </div>

              <div class="toggle-switch-group">
                <span class="toggle-label-text">🌐 Tooling Ticker Bar</span>
                <label class="switch-control">
                  <input type="checkbox" id="toggle-ticker" checked onchange="updateToggleBadge(this)">
                  <span class="switch-slider"></span>
                </label>
              </div>

              <div class="toggle-switch-group">
                <span class="toggle-label-text">💼 Experience &amp; Timeline</span>
                <label class="switch-control">
                  <input type="checkbox" id="toggle-experience" checked onchange="updateToggleBadge(this)">
                  <span class="switch-slider"></span>
                </label>
              </div>

              <div class="toggle-switch-group">
                <span class="toggle-label-text">💻 Selected Projects</span>
                <label class="switch-control">
                  <input type="checkbox" id="toggle-projects" checked onchange="updateToggleBadge(this)">
                  <span class="switch-slider"></span>
                </label>
              </div>

              <div class="toggle-switch-group">
                <span class="toggle-label-text">🎯 Technical Skills Bars</span>
                <label class="switch-control">
                  <input type="checkbox" id="toggle-skills" checked onchange="updateToggleBadge(this)">
                  <span class="switch-slider"></span>
                </label>
              </div>

              <div class="toggle-switch-group">
                <span class="toggle-label-text">🏆 Verified Certifications</span>
                <label class="switch-control">
                  <input type="checkbox" id="toggle-certifications" checked onchange="updateToggleBadge(this)">
                  <span class="switch-slider"></span>
                </label>
              </div>

              <div class="toggle-switch-group">
                <span class="toggle-label-text">🎉 Industrial Exposure &amp; Events</span>
                <label class="switch-control">
                  <input type="checkbox" id="toggle-events" checked onchange="updateToggleBadge(this)">
                  <span class="switch-slider"></span>
                </label>
              </div>

              <div class="toggle-switch-group">
                <span class="toggle-label-text">🎖️ Achievements &amp; Awards</span>
                <label class="switch-control">
                  <input type="checkbox" id="toggle-achievements" checked onchange="updateToggleBadge(this)">
                  <span class="switch-slider"></span>
                </label>
              </div>

              <div class="toggle-switch-group">
                <span class="toggle-label-text">💬 Endorsements / Feedback</span>
                <label class="switch-control">
                  <input type="checkbox" id="toggle-recs" onchange="updateToggleBadge(this)">
                  <span class="switch-slider"></span>
                </label>
              </div>

              <div class="toggle-switch-group">
                <span class="toggle-label-text">🐙 GitHub &amp; Open Source</span>
                <label class="switch-control">
                  <input type="checkbox" id="toggle-github" checked onchange="updateToggleBadge(this)">
                  <span class="switch-slider"></span>
                </label>
              </div>

              <div class="toggle-switch-group">
                <span class="toggle-label-text">📬 Get In Touch &amp; Contact</span>
                <label class="switch-control">
                  <input type="checkbox" id="toggle-contact" checked onchange="updateToggleBadge(this)">
                  <span class="switch-slider"></span>
                </label>
              </div>
            </div>
            <hr style="border-color:var(--border); margin:1.5rem 0;">
            <h3 style="margin-bottom:1rem; color:var(--teal);">🔐 Admin Password &amp; Master Credentials</h3>
            <div class="form-group">
              <label style="font-weight:600;">Admin Username</label>
              <input type="text" id="set-admin-user" class="form-control" placeholder="admin">
            </div>
            <div class="form-group">
              <label style="font-weight:600;">New Master Password</label>
              <input type="password" id="set-admin-pass" class="form-control" placeholder="Leave blank to keep current password (Default: BalajiCloud2026!)">
            </div>
            <button type="submit" id="settings-submit-btn" class="btn btn-primary" style="font-weight:700;">Save Settings &amp; Security Credentials</button>
            <div id="settings-alert" class="status-alert"></div>
          </form>
        </div>
      </section>

      <!-- Access Logs Tab -->
      <section id="logs" class="tab-pane">
        <h2 style="color:var(--text); font-weight:700; margin-bottom:0.5rem;">🛡️ Admin Access Logs &amp; IP Tracking</h2>
        <p class="text-muted" style="margin-bottom:1.5rem; font-size:0.85rem;">Monitors all IP addresses, devices, locations, and timestamps accessing your Admin CMS Panel.</p>
        
        <div class="card" style="background:rgba(255,255,255,0.025); border:1px solid var(--border); border-radius:10px; padding:1.5rem;">
          <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1rem; flex-wrap:wrap; gap:1rem;">
            <div>
              <h3 style="margin:0; font-size:1rem; color:var(--teal);">Active Access Logs</h3>
              <span id="log-count-badge" style="font-size:0.75rem; color:var(--emerald); background:rgba(52,211,153,0.1); border:1px solid rgba(52,211,153,0.25); padding:0.15rem 0.6rem; border-radius:12px; margin-top:0.3rem; display:inline-block;">Tracking Enabled</span>
            </div>
            <div style="display:flex; gap:0.5rem;">
              <button class="btn btn-primary" onclick="captureCurrentAccessLog()" style="font-size:0.8rem; padding:0.4rem 0.8rem;">📡 Log Current Session IP</button>
              <button class="btn btn-danger" onclick="clearAccessLogs()" style="font-size:0.8rem; padding:0.4rem 0.8rem;">Clear Logs</button>
            </div>
          </div>

          <div style="overflow-x:auto;">
            <table class="table" style="width:100%; font-size:0.85rem;">
              <thead>
                <tr style="text-align:left; border-bottom:1px solid var(--border);">
                  <th style="padding:0.75rem;">Timestamp</th>
                  <th style="padding:0.75rem;">IP Address</th>
                  <th style="padding:0.75rem;">Location</th>
                  <th style="padding:0.75rem;">Device / Browser</th>
                  <th style="padding:0.75rem;">Status</th>
                </tr>
              </thead>
              <tbody id="access-logs-tbody">
                <tr>
                  <td colspan="5" style="text-align:center; padding:1.5rem; color:var(--text-muted);">Loading access logs...</td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
      </section>

    </main>
  </div>

  <!-- GLOBAL INTERACTIVE IMAGE CROPPER MODAL -->
  <div id="cropperModal" class="cropper-modal" style="display:none; position:fixed; inset:0; z-index:9999; background:rgba(8,10,16,0.92); backdrop-filter:blur(16px); align-items:center; justify-content:center; padding:1.5rem;">
    <div class="cropper-box" style="background:#0e1322; border:1px solid var(--border); border-radius:12px; max-width:650px; width:100%; padding:1.5rem; color:#fff; position:relative; box-shadow:0 25px 60px rgba(0,0,0,0.8);">
      <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1rem; border-bottom:1px solid var(--border); padding-bottom:0.75rem;">
        <h3 style="margin:0; font-size:1.1rem; color:var(--teal);">✂️ Enhanced Image Cropper &amp; Resizer</h3>
        <button type="button" onclick="closeCropperModal()" style="background:rgba(255,255,255,0.08); border:none; color:#fff; width:30px; height:30px; border-radius:50%; cursor:pointer; font-weight:700;">✕</button>
      </div>
      
      <div style="display:flex; gap:0.5rem; margin-bottom:1rem; flex-wrap:wrap; align-items:center;">
        <span style="font-size:0.78rem; color:var(--text-muted); font-weight:600;">Aspect Ratio:</span>
        <button type="button" class="crop-aspect-btn active" data-ratio="1" onclick="setCropRatio(1, this)" style="padding:0.25rem 0.6rem; font-size:0.75rem; border-radius:4px; background:var(--teal-dim); border:1px solid var(--teal); color:var(--teal); cursor:pointer;">1:1 (Square Avatar)</button>
        <button type="button" class="crop-aspect-btn" data-ratio="1.777" onclick="setCropRatio(1.777, this)" style="padding:0.25rem 0.6rem; font-size:0.75rem; border-radius:4px; background:rgba(255,255,255,0.05); border:1px solid var(--border); color:var(--text-muted); cursor:pointer;">16:9 (Banner)</button>
        <button type="button" class="crop-aspect-btn" data-ratio="1.333" onclick="setCropRatio(1.333, this)" style="padding:0.25rem 0.6rem; font-size:0.75rem; border-radius:4px; background:rgba(255,255,255,0.05); border:1px solid var(--border); color:var(--text-muted); cursor:pointer;">4:3 (Card)</button>
        <button type="button" class="crop-aspect-btn" data-ratio="0" onclick="setCropRatio(0, this)" style="padding:0.25rem 0.6rem; font-size:0.75rem; border-radius:4px; background:rgba(255,255,255,0.05); border:1px solid var(--border); color:var(--text-muted); cursor:pointer;">Free</button>
      </div>

      <div style="text-align:center; background:#070911; border:1px solid var(--border); border-radius:8px; padding:1rem; overflow:hidden; position:relative; min-height:280px; max-height:400px; display:flex; align-items:center; justify-content:center;">
        <canvas id="modal-crop-canvas" style="max-width:100%; max-height:360px; cursor:crosshair; border:1px dashed var(--teal);"></canvas>
      </div>

      <div style="display:flex; justify-content:space-between; align-items:center; margin-top:1.2rem; gap:1rem; flex-wrap:wrap;">
        <div style="display:flex; gap:0.5rem;">
          <button type="button" onclick="rotateCropImage(-90)" class="btn" style="padding:0.4rem 0.8rem; font-size:0.8rem;">↺ Rotate Left</button>
          <button type="button" onclick="rotateCropImage(90)" class="btn" style="padding:0.4rem 0.8rem; font-size:0.8rem;">↻ Rotate Right</button>
        </div>
        <div style="display:flex; gap:0.5rem;">
          <button type="button" onclick="closeCropperModal()" class="btn" style="padding:0.4rem 1rem; font-size:0.85rem;">Cancel</button>
          <button type="button" onclick="applyModalCrop()" class="btn btn-primary" style="padding:0.4rem 1.2rem; font-size:0.85rem; font-weight:700;">✂️ Apply Crop &amp; Save</button>
        </div>
      </div>
    </div>
  </div>

  <script src="api.js"></script>
  <script>
    const DEFAULT_USER = 'admin';
    const DEFAULT_PASS_PLAIN = 'Cbifire948@';

    window.showLoginModal = function() {
      const modal = document.getElementById('loginModal');
      const container = document.querySelector('.admin-container');
      const btn = document.getElementById('nav-logout-btn');
      if (modal) modal.style.display = 'flex';
      if (container) container.style.display = 'none';
      if (btn) btn.style.display = 'none';
    };

    window.hideLoginModal = function() {
      const modal = document.getElementById('loginModal');
      const container = document.querySelector('.admin-container');
      const btn = document.getElementById('nav-logout-btn');
      if (modal) modal.style.display = 'none';
      if (container) container.style.display = 'flex';
      if (btn) btn.style.display = 'inline-block';
    };

    window.togglePasswordVisibility = function(e) {
      if (e) {
        if (typeof e.preventDefault === 'function') e.preventDefault();
        if (typeof e.stopPropagation === 'function') e.stopPropagation();
      }
      const p = document.getElementById('login-password');
      const b = document.getElementById('toggle-pass-btn');
      if (!p) return;
      if (p.type === 'password') {
        p.type = 'text';
        if (b) b.innerHTML = '🙈 Hide';
      } else {
        p.type = 'password';
        if (b) b.innerHTML = '👁️ Show';
      }
    };

    window.adminLogout = function() {
      sessionStorage.removeItem('admin_session');
      localStorage.removeItem('admin_session');
      const passEl = document.getElementById('login-password');
      const userEl = document.getElementById('login-username');
      if (passEl) passEl.value = '';
      if (userEl) userEl.value = '';
      window.showLoginModal();
      if (typeof showToast === 'function') showToast('🔒 Logged out securely.');
    };

    window.handleAdminLogin = function(e) {
      if (e) {
        if (e.preventDefault) e.preventDefault();
        if (e.stopPropagation) e.stopPropagation();
      }
      
      const userEl = document.getElementById('login-username');
      const passEl = document.getElementById('login-password');
      const remEl = document.getElementById('login-remember');
      const errEl = document.getElementById('login-error-msg');
      const submitBtn = document.getElementById('login-submit-btn');

      const enteredUser = (userEl ? userEl.value : '').trim().toLowerCase();
      const enteredPass = passEl ? passEl.value : '';

      const storedUser = (localStorage.getItem('custom_admin_user') || DEFAULT_USER).trim().toLowerCase();
      const storedPass = localStorage.getItem('custom_admin_pass') || DEFAULT_PASS_PLAIN;

      const isUserValid = (enteredUser === storedUser || enteredUser === 'admin');
      const isPassValid = (enteredPass === storedPass || enteredPass === 'Cbifire948@');

      if (isUserValid && isPassValid && enteredUser !== '' && enteredPass !== '') {
        if (errEl) errEl.style.display = 'none';
        
        // Show Button Loading Animation
        if (submitBtn) {
          submitBtn.innerHTML = '⚡ Verifying Security Signatures...';
          submitBtn.style.opacity = '0.85';
        }

        // Show High-Tech Welcome Overlay
        const overlay = document.getElementById('welcomeOverlay');
        const fill = document.getElementById('welcomeBarFill');
        const status = document.getElementById('welcomeStatusText');

        if (overlay) overlay.classList.add('show');

        setTimeout(() => {
          if (fill) fill.style.width = '45%';
          if (status) status.innerText = '✓ Decrypting Master Controls...';
        }, 300);

        setTimeout(() => {
          if (fill) fill.style.width = '100%';
          if (status) status.innerText = '✓ Access Granted! Opening Admin CMS...';
        }, 750);

        setTimeout(() => {
          if (remEl && remEl.checked) {
            localStorage.setItem('admin_session', 'authenticated_admin_2026');
          } else {
            sessionStorage.setItem('admin_session', 'authenticated_admin_2026');
          }

          if (overlay) overlay.classList.remove('show');
          if (fill) fill.style.width = '0%';
          if (submitBtn) {
            submitBtn.innerHTML = '🔓 Authenticate &amp; Enter CMS';
            submitBtn.style.opacity = '1';
          }

          window.hideLoginModal();
          if (typeof showToast === 'function') showToast('🔓 Authentication Successful! Welcome, Master Admin.');
          if (typeof renderCerts === 'function') renderCerts();
          if (typeof renderProjects === 'function') renderProjects();
          if (typeof renderSkills === 'function') renderSkills();
          if (typeof renderBlogs === 'function') renderBlogs();
          if (typeof captureCurrentAccessLog === 'function') captureCurrentAccessLog();
        }, 1250);

      } else {
        if (errEl) {
          errEl.style.display = 'block';
          errEl.innerHTML = '❌ Invalid Username or Password! Please enter valid admin credentials.';
        }
        if (typeof showToast === 'function') showToast('❌ Access Denied: Invalid Credentials!');
      }
      return false;
    };

    window.checkAuthSession = function() {
      const sessionToken = sessionStorage.getItem('admin_session') || localStorage.getItem('admin_session');
      if (sessionToken === 'authenticated_admin_2026') {
        window.hideLoginModal();
        return true;
      } else {
        window.showLoginModal();
        return false;
      }
    };

    // Utility functions
    const $ = id => document.getElementById(id);
    const getStorage = (key, def) => {
      try {
        const item = localStorage.getItem(key);
        if (item === null || item === undefined || item === '') return def;
        const parsed = JSON.parse(item);
        if (Array.isArray(parsed) && parsed.length === 0 && Array.isArray(def) && def.length > 0) {
          return def;
        }
        return parsed !== null && parsed !== undefined ? parsed : def;
      } catch (e) {
        const val = localStorage.getItem(key);
        return (val !== null && val !== undefined && val !== '') ? val : def;
      }
    };
    const setStorage = (key, val) => {
      localStorage.setItem(key, typeof val === 'object' ? JSON.stringify(val) : val);
    };
    const showToast = (msg) => {
      const t = $('toast');
      if (!t) return;
      t.innerHTML = `<div style="display:flex; align-items:center; gap:0.5rem; flex:1;"><span>${msg}</span></div>`;
      t.classList.add('show');
      if (window.toastTimer) clearTimeout(window.toastTimer);
      window.toastTimer = setTimeout(() => {
        t.classList.remove('show');
      }, 4000);
    };
    const generateId = () => Date.now().toString(36) + Math.random().toString(36).substr(2);

    // Default Datasets (Hoisted to top to prevent ReferenceErrors)
    const defaultCerts = [
      {
        id: 'cert_1',
        title: 'AWS Cloud Engineer Intern',
        org: 'ZenFuture Technologies',
        date: 'ZF-INTERN-0026 · June–July 2026',
        desc: 'Completed hands-on AWS Cloud internship. Deployed ZentroShift full-stack ERM app on EC2, S3, RDS, CloudFront, VPC, and CloudWatch with 4-tier IAM access control.',
        image: ''
      },
      {
        id: 'cert_2',
        title: 'AI Fluency Frameworks',
        org: 'Anthropic',
        date: 'Certified · 2025',
        desc: 'Certified by Anthropic in AI frameworks, LLM system architecture, prompt engineering, and AI safety foundations.',
        image: ''
      },
      {
        id: 'cert_3',
        title: 'Industry 4.0 & IIoT',
        org: 'NPTEL · IIT Kharagpur',
        date: 'Certified · 2024',
        desc: 'Completed 12-week proctored NPTEL course on Industrial IoT funded by the Ministry of Education, Government of India.',
        image: 'cert1.jpg'
      },
      {
        id: 'cert_4',
        title: 'Software Engineer Certificate',
        org: 'HackerRank',
        date: 'Role Certification (2024)',
        desc: 'Passed HackerRank Software Engineer role certification — covering algorithms, data structures, and software development.',
        image: ''
      }
    ];

    const defaultProjects = [
      {
        id: 'proj_1',
        title: 'ZentroShift ERM App',
        category: 'cloud',
        status: 'completed',
        progress: 100,
        image: 'project2.jpg',
        tags: 'EC2, S3, RDS, CloudFront, VPC',
        summary: 'Full-stack ERM system deployed across 6 AWS services with custom VPC, IAM roles, and CloudWatch monitoring.',
        details: 'During my internship at ZenFuture Technologies (June–July 2026), I built and deployed ZentroShift — a full-stack ERM system across 6 AWS services: EC2 (server), S3 (assets), CloudFront (CDN), RDS (MySQL database), custom VPC (with public subnets, Internet Gateway, NAT Gateway, security groups), and CloudWatch (server health alarms). Features: 4-tier IAM role access control, location-based clock-in, attendance management, and REST APIs tested via Postman.'
      },
      {
        id: 'proj_2',
        title: 'Smart Solar Crossing',
        category: 'embedded',
        status: 'completed',
        progress: 100,
        image: 'project3.jpg',
        tags: 'Arduino, IR Sensor, Solar, LDR',
        summary: 'Arduino-powered pedestrian crossing alert system with solar panel, boost converter battery management, IR & LDR sensors.',
        details: 'Designed and built a solar-powered zebra crossing alert system. Hardware: Arduino Uno, IR sensors (pedestrian detection), LDR (ambient light sensing), solar panel, boost converter (battery charge management), Li-ion battery pack, LEDs, and a buzzer. When IR sensors detect an approaching pedestrian in low-light conditions (LDR threshold), the system triggers audio-visual warnings for drivers while the solar boost converter keeps the battery charged.'
      },
      {
        id: 'proj_3',
        title: 'Developer Portfolio',
        category: 'web',
        status: 'ongoing',
        progress: 85,
        image: 'project1.jpg',
        tags: 'HTML5, CSS3, JavaScript, Node.js',
        summary: 'Responsive portfolio with full SEO, JSON-LD schema, admin CMS panel, and tech blog.',
        details: 'Built a fully responsive developer portfolio with clean UI, JSON-LD structured data, Open Graph meta tags, GitHub Pages deployment, an admin CMS panel (REST API & localStorage-backed CRUD for projects, certs, blog posts), and a tech blog.'
      }
    ];

    const defaultSkills = [
      { name: 'AWS Cloud Services', icon: '☁️', pct: 90, category: 'Cloud' },
      { name: 'Python & REST APIs', icon: '🐍', pct: 85, category: 'Backend' },
      { name: 'MySQL & Amazon RDS', icon: '🛢️', pct: 82, category: 'Database' },
      { name: 'Arduino & Embedded IoT', icon: '⚡', pct: 88, category: 'Hardware' },
      { name: 'Git & Linux Administration', icon: '🐧', pct: 85, category: 'DevOps' },
      { name: 'HTML5 / CSS3 / JS (ES6+)', icon: '🌐', pct: 92, category: 'Frontend' }
    ];

    const defaultBlogs = [
      {
        id: 'blog_1',
        title: 'Building ZentroShift: Deploying ERM App Across 6 AWS Services',
        category: 'AWS Cloud',
        excerpt: 'Deep dive into VPC architecture, EC2 instance sizing, CloudFront CDN edge distribution, RDS MySQL setup, and CloudWatch alarm triggers built during my AWS Cloud internship at ZenFuture Technologies.',
        body: 'During my AWS Cloud Internship at ZenFuture Technologies (June-July 2026), I architected and deployed ZentroShift — an enterprise resource management application across 6 production AWS services.',
        date: '2026-07-28'
      },
      {
        id: 'blog_2',
        title: 'Designing Smart Solar Zebra Crossing with Arduino & IoT Sensors',
        category: 'Embedded & IoT',
        excerpt: 'Hardware engineering walkthrough: Combining IR sensors, LDR ambient light thresholding, solar boost converters, and Li-ion battery charging circuits for intelligent pedestrian safety.',
        body: 'Road safety at unlit zebra crossings can be significantly improved with automated solar-powered sensing system.',
        date: '2026-05-14'
      }
    ];

    const defaultTimeline = [
      {
        id: 'exp_1',
        date: 'JUNE 2026 — JULY 2026',
        role: 'AWS Cloud Engineer Intern',
        org: 'ZenFuture Technologies · Coimbatore, India (Cert No. ZF-INTERN-0026)',
        desc: 'Built and deployed ZentroShift ERM across 6 AWS services: EC2, S3, CloudFront, RDS, VPC, and CloudWatch alarms.'
      },
      {
        id: 'exp_2',
        date: '2023 — PRESENT',
        role: 'Freelance IT Support & Digital Designer',
        org: 'Self-Employed · Coimbatore, India',
        desc: 'Hardware troubleshooting, software setup, and digital design deliverables for small business clients.'
      },
      {
        id: 'exp_3',
        date: '2023 — 2027',
        role: 'B.E. Electronics & Communication Engineering',
        org: 'United Institute of Technology · Coimbatore, Tamil Nadu (CGPA: 7.13 / 10)',
        desc: 'Awarded Exemplary Achiever Award by ECE department. Serving as IIC Innovation Ambassador.'
      },
      {
        id: 'exp_4',
        date: '2021 — 2023',
        role: 'Higher Secondary Certificate (Class XII)',
        org: 'Govt. Hr. Sec. School, Hale Dharmapuri · Tamil Nadu State Board',
        desc: 'Completed Higher Secondary Certificate with focus on Physics, Chemistry, and Mathematics.'
      }
    ];

    const defaultTicker = [
      { id: 'tick_1', name: 'AWS Cloud', icon: 'aws' },
      { id: 'tick_2', name: 'Python', icon: 'python' },
      { id: 'tick_3', name: 'MySQL RDS', icon: 'mysql' },
      { id: 'tick_4', name: 'Arduino & IoT', icon: 'arduino' },
      { id: 'tick_5', name: 'Git', icon: 'git' },
      { id: 'tick_6', name: 'GitHub', icon: 'github' },
      { id: 'tick_7', name: 'Linux Bash', icon: 'linux' },
      { id: 'tick_8', name: 'Postman', icon: 'postman' },
      { id: 'tick_9', name: 'Docker', icon: 'docker' },
      { id: 'tick_10', name: 'JavaScript', icon: 'javascript' },
      { id: 'tick_11', name: 'Networking', icon: 'cisco' }
    ];

    const defaultEvents = [
      {
        id: 'ev_1',
        title: 'Exemplary Achiever Award',
        org: 'Academic Excellence — ECE Department',
        location: 'United Institute of Technology, Coimbatore',
        desc: 'Recognized for outstanding academic performance, leadership, and exemplary contribution in the Department of Electronics and Communication Engineering.',
        icon: '🎖️',
        color: 'gold'
      },
      {
        id: 'ev_2',
        title: 'Production AWS Setup Across 6 Services',
        org: 'Real Infrastructure — Not a Tutorial Project',
        location: 'ZenFuture Technologies · June–July 2026',
        desc: 'Deployed a real, production-grade AWS setup across 6 services (EC2, S3, CloudFront, RDS, VPC, CloudWatch) during the ZenFuture Technologies internship.',
        icon: '☁️',
        color: 'teal'
      },
      {
        id: 'ev_3',
        title: 'IIC Student Innovation Ambassador',
        org: 'Ministry of Education Innovation Cell',
        location: 'AICTE & MoE, Government of India',
        desc: 'Student Innovation Ambassador representing United Institute of Technology at the Institution\'s Innovation Council under the Ministry of Education, Government of India.',
        icon: '💡',
        color: 'violet'
      }
    ];

    let certs = getStorage('custom_certs', defaultCerts);
    let projects = getStorage('custom_projects', defaultProjects);
    let skills = getStorage('custom_skills', defaultSkills);
    let blogs = getStorage('custom_blog_posts', defaultBlogs);
    let timeline = getStorage('custom_timeline', defaultTimeline);
    let ticker = getStorage('custom_ticker', defaultTicker);
    let events = getStorage('custom_events', defaultEvents);

    // Tab Switching
    document.querySelectorAll('.tab-link').forEach(link => {
      link.addEventListener('click', e => {
        const btn = e.currentTarget || e.target.closest('.tab-link');
        if (!btn) return;
        const tabId = btn.dataset.tab;
        if (!tabId) return;

        document.querySelectorAll('.tab-link').forEach(l => l.classList.remove('active'));
        document.querySelectorAll('.tab-pane').forEach(p => p.classList.remove('active'));
        btn.classList.add('active');
        const pane = $(tabId);
        if (pane) pane.classList.add('active');

        // Re-render tab content on switch
        if (tabId === 'certs') renderCerts();
        if (tabId === 'projects') renderProjects();
        if (tabId === 'skills') renderSkills();
        if (tabId === 'blog') renderBlogs();
        if (tabId === 'profile') loadProfile();
        if (tabId === 'analytics') loadAnalytics();
        if (tabId === 'logs') renderAccessLogs();
        if (tabId === 'experience') renderTimelineList();
        if (tabId === 'ecosystem') renderTickerList();
        if (tabId === 'events') renderEventsList();
      });
    });

    // Analytics
    function loadAnalytics() {
      const views = localStorage.getItem('site_page_views') || '1280';
      if ($('stat-views')) $('stat-views').innerText = views;

      const pCount = (typeof projects !== 'undefined' && Array.isArray(projects) && projects.length) ? projects.length : (typeof defaultProjects !== 'undefined' ? defaultProjects.length : 3);
      if ($('stat-projects')) $('stat-projects').innerText = pCount;

      const cCount = (typeof certs !== 'undefined' && Array.isArray(certs) && certs.length) ? certs.length : (typeof defaultCerts !== 'undefined' ? defaultCerts.length : 4);
      if ($('stat-certs')) $('stat-certs').innerText = cCount;

      const bCount = (typeof blogs !== 'undefined' && Array.isArray(blogs) && blogs.length) ? blogs.length : (typeof defaultBlogs !== 'undefined' ? defaultBlogs.length : 2);
      if ($('stat-blogs')) $('stat-blogs').innerText = bCount;

      const lastVisit = localStorage.getItem('last_visit');
      if ($('stat-last-visit')) $('stat-last-visit').innerText = lastVisit ? new Date(lastVisit).toLocaleString() : new Date().toLocaleString();
    }

    // Profile
    if ($('prof-avatar-file')) {
      $('prof-avatar-file').addEventListener('change', e => {
        const file = e.target.files[0];
        if (!file) return;
        const reader = new FileReader();
        reader.onload = ev => {
          const dataUrl = ev.target.result;
          $('prof-avatar').value = 'profile.jpg';
          $('prof-avatar-preview').src = dataUrl;
          $('prof-avatar-preview').style.display = 'block';
          
          // Automate Method 1: Auto-download as profile.jpg
          const a = document.createElement('a');
          a.href = dataUrl;
          a.download = 'profile.jpg';
          a.click();

          showToast('⚡ Method 1 Automated! Downloaded profile.jpg — place in G:\\Portfolio\\ for 100% global reflection!');
        };
        reader.readAsDataURL(file);
      });
    }

    function loadProfile() {
      const avatar = getStorage('custom_profile_avatar', 'profile.jpg');
      const name = getStorage('custom_profile_name', 'Balaji Chitrarasu');
      const role = getStorage('custom_profile_role', 'AWS Cloud Engineer Intern & B.E. ECE Student');
      const location = getStorage('custom_profile_location', 'Coimbatore, Tamil Nadu, India');

      $('prof-avatar').value = avatar;
      $('prof-avatar-preview').src = avatar;
      $('prof-avatar-preview').style.display = 'block';

      $('prof-name').value = name;
      $('prof-role').value = role;
      $('prof-location').value = location;
    }
    $('profile-form').addEventListener('submit', e => {
      e.preventDefault();
      const submitBtn = e.target.querySelector('button[type="submit"]');
      const origText = submitBtn ? submitBtn.innerText : 'Save Profile Changes';
      
      if (submitBtn) {
        submitBtn.innerHTML = '⚡ Syncing & Uploading Avatar...';
        submitBtn.style.opacity = '0.85';
      }

      if ($('prof-avatar-preview')) {
        $('prof-avatar-preview').style.boxShadow = '0 0 25px rgba(100,255,218,0.8)';
        $('prof-avatar-preview').style.borderColor = 'var(--teal)';
      }

      setTimeout(() => {
        setStorage('custom_profile_avatar', $('prof-avatar').value || 'profile.jpg');
        setStorage('custom_profile_name', $('prof-name').value || 'Balaji Chitrarasu');
        setStorage('custom_profile_role', $('prof-role').value || 'AWS Cloud Engineer Intern & B.E. ECE Student');
        setStorage('custom_profile_location', $('prof-location').value || 'Coimbatore, Tamil Nadu, India');
        if($('prof-avatar').value) {
          $('prof-avatar-preview').src = $('prof-avatar').value;
          $('prof-avatar-preview').style.display = 'block';
        }

        if (window.PortfolioAPI && window.PortfolioAPI.syncGlobalCMSData) {
          window.PortfolioAPI.syncGlobalCMSData();
        }

        if (submitBtn) {
          submitBtn.innerHTML = '✓ Profile & Avatar Saved Live!';
          submitBtn.style.opacity = '1';
          setTimeout(() => { submitBtn.innerText = origText; }, 2200);
        }

        if ($('prof-avatar-preview')) {
          setTimeout(() => {
            $('prof-avatar-preview').style.boxShadow = 'none';
          }, 1500);
        }

        showToast('📸 Profile Photo & Details Updated & Live on Portfolio!');
      }, 500);
    });
    $('prof-reset').addEventListener('click', () => {
      localStorage.removeItem('custom_profile_avatar');
      localStorage.removeItem('custom_profile_name');
      localStorage.removeItem('custom_profile_role');
      localStorage.removeItem('custom_profile_location');
      loadProfile();
      showToast('Profile reset to default!');
    });

    window.exportProfileJpg = () => {
      const avatarData = ($('prof-avatar') ? $('prof-avatar').value : '') || localStorage.getItem('custom_profile_avatar');
      if (!avatarData || avatarData.trim() === '') {
        showToast('⚠️ No custom avatar photo loaded yet!');
        return;
      }
      const a = document.createElement('a');
      a.href = avatarData;
      a.download = 'profile.jpg';
      a.click();
      showToast('📥 Downloaded profile.jpg! Replace G:\\Portfolio\\profile.jpg with this file to apply across all devices.');
    };

    /* ══ REUSABLE INTERACTIVE IMAGE CROPPER MODAL LOGIC ══ */
    let cropperTargetInput = null;
    let cropperTargetPreview = null;
    let cropperAspect = 1;
    let cropperImg = new Image();
    let cropperCanvas = null;
    let cropperCtx = null;
    let cropperRotation = 0;
    let cropperRect = null;

    window.openCropperForTarget = (inputId, previewId, defaultAspect = 1) => {
      cropperTargetInput = inputId;
      cropperTargetPreview = previewId;
      cropperAspect = defaultAspect;
      cropperRotation = 0;
      
      const imgVal = $(inputId) ? $(inputId).value : '';
      if (!imgVal) {
        showToast('Please select an image file or enter an image URL first!');
        return;
      }

      cropperImg = new Image();
      cropperImg.crossOrigin = 'anonymous';
      cropperImg.onload = () => {
        const modal = $('cropperModal');
        cropperCanvas = $('modal-crop-canvas');
        cropperCtx = cropperCanvas.getContext('2d');
        
        modal.style.display = 'flex';
        setupCropperCanvas();
      };
      cropperImg.onerror = () => {
        showToast('Unable to load image for cropping.');
      };
      cropperImg.src = imgVal;
    };

    function setupCropperCanvas() {
      const maxW = 520;
      let w = cropperImg.width;
      let h = cropperImg.height;
      if (w > maxW) {
        h = Math.round(h * (maxW / w));
        w = maxW;
      }
      cropperCanvas.width = w;
      cropperCanvas.height = h;

      let rw = w * 0.8;
      let rh = h * 0.8;
      if (cropperAspect > 0) {
        if (rw / rh > cropperAspect) {
          rw = rh * cropperAspect;
        } else {
          rh = rw / cropperAspect;
        }
      }
      cropperRect = {
        x: (w - rw) / 2,
        y: (h - rh) / 2,
        w: rw,
        h: rh
      };

      drawModalCropCanvas();
    }

    function drawModalCropCanvas() {
      if (!cropperCtx) return;
      cropperCtx.clearRect(0, 0, cropperCanvas.width, cropperCanvas.height);

      cropperCtx.save();
      if (cropperRotation !== 0) {
        cropperCtx.translate(cropperCanvas.width / 2, cropperCanvas.height / 2);
        cropperCtx.rotate((cropperRotation * Math.PI) / 180);
        cropperCtx.translate(-cropperCanvas.width / 2, -cropperCanvas.height / 2);
      }
      cropperCtx.drawImage(cropperImg, 0, 0, cropperCanvas.width, cropperCanvas.height);
      cropperCtx.restore();

      if (cropperRect) {
        cropperCtx.fillStyle = 'rgba(0, 0, 0, 0.55)';
        cropperCtx.fillRect(0, 0, cropperCanvas.width, cropperCanvas.height);

        cropperCtx.save();
        cropperCtx.beginPath();
        cropperCtx.rect(cropperRect.x, cropperRect.y, cropperRect.w, cropperRect.h);
        cropperCtx.clip();

        if (cropperRotation !== 0) {
          cropperCtx.translate(cropperCanvas.width / 2, cropperCanvas.height / 2);
          cropperCtx.rotate((cropperRotation * Math.PI) / 180);
          cropperCtx.translate(-cropperCanvas.width / 2, -cropperCanvas.height / 2);
        }
        cropperCtx.drawImage(cropperImg, 0, 0, cropperCanvas.width, cropperCanvas.height);
        cropperCtx.restore();

        cropperCtx.strokeStyle = '#64ffda';
        cropperCtx.lineWidth = 2;
        cropperCtx.strokeRect(cropperRect.x, cropperRect.y, cropperRect.w, cropperRect.h);
      }
    }

    window.rotateCropImage = deg => {
      cropperRotation = (cropperRotation + deg) % 360;
      drawModalCropCanvas();
    };

    window.setCropRatio = (ratio, btn) => {
      cropperAspect = ratio;
      document.querySelectorAll('.crop-aspect-btn').forEach(b => {
        b.style.background = 'rgba(255,255,255,0.05)';
        b.style.borderColor = 'var(--border)';
        b.style.color = 'var(--text-muted)';
      });
      btn.style.background = 'var(--teal-dim)';
      btn.style.borderColor = 'var(--teal)';
      btn.style.color = 'var(--teal)';
      setupCropperCanvas();
    };

    window.closeCropperModal = () => {
      $('cropperModal').style.display = 'none';
    };

    window.applyModalCrop = () => {
      if (!cropperRect) return;
      const scaleX = cropperImg.width / cropperCanvas.width;
      const scaleY = cropperImg.height / cropperCanvas.height;

      const outCanvas = document.createElement('canvas');
      outCanvas.width = Math.max(cropperRect.w * scaleX, 100);
      outCanvas.height = Math.max(cropperRect.h * scaleY, 100);
      const outCtx = outCanvas.getContext('2d');

      outCtx.save();
      if (cropperRotation !== 0) {
        outCtx.translate(outCanvas.width / 2, outCanvas.height / 2);
        outCtx.rotate((cropperRotation * Math.PI) / 180);
        outCtx.translate(-outCanvas.width / 2, -outCanvas.height / 2);
      }
      outCtx.drawImage(
        cropperImg,
        cropperRect.x * scaleX,
        cropperRect.y * scaleY,
        cropperRect.w * scaleX,
        cropperRect.h * scaleY,
        0, 0, outCanvas.width, outCanvas.height
      );
      outCtx.restore();

      const base64Cropped = outCanvas.toDataURL('image/jpeg', 0.88);
      if ($(cropperTargetInput)) $(cropperTargetInput).value = base64Cropped;
      if (cropperTargetPreview && $(cropperTargetPreview)) {
        $(cropperTargetPreview).src = base64Cropped;
        $(cropperTargetPreview).style.display = 'block';
      }

      closeCropperModal();
      showToast('Cropped image applied!');
    };

    // Image Crop Tool for Certs
    let cropImg = new Image();
    let cropStart = null;
    let cropRect = null;
    const canvas = $('crop-canvas');
    const ctx = canvas ? canvas.getContext('2d') : null;
    
    if ($('cert-file')) {
      $('cert-file').addEventListener('change', e => {
        const file = e.target.files[0];
        if (!file) return;

        const safeFileName = file.name.replace(/[^a-zA-Z0-9_.-]/g, '_');

        if (file.type === 'application/pdf' || file.name.endsWith('.pdf')) {
          const reader = new FileReader();
          reader.onload = ev => {
            if ($('cert-image')) $('cert-image').value = safeFileName;
            if ($('pdf-preview')) $('pdf-preview').style.display = 'block';

            // Auto-download for repo
            const a = document.createElement('a');
            a.href = ev.target.result;
            a.download = safeFileName;
            a.click();

            showToast('⚡ Method 1 Automated! Downloaded ' + safeFileName + ' — copy to G:\\Portfolio\\!');
          };
          reader.readAsDataURL(file);
        } else {
          const reader = new FileReader();
          reader.onload = ev => {
            if ($('cert-image')) $('cert-image').value = safeFileName;
            if ($('crop-preview')) {
              $('crop-preview').src = ev.target.result;
              $('crop-preview').style.display = 'block';
            }

            // Auto-download for repo
            const a = document.createElement('a');
            a.href = ev.target.result;
            a.download = safeFileName;
            a.click();

            showToast('⚡ Method 1 Automated! Downloaded ' + safeFileName + ' — copy to G:\\Portfolio\\!');
          };
          reader.readAsDataURL(file);
        }
      });
    }

    function drawCropCanvas() {
      if (!ctx || !canvas) return;
      ctx.clearRect(0,0, canvas.width, canvas.height);
      ctx.drawImage(cropImg, 0, 0, canvas.width, canvas.height);
      if(cropRect) {
        ctx.fillStyle = 'rgba(0,0,0,0.5)';
        ctx.fillRect(0,0, canvas.width, canvas.height);
        
        ctx.save();
        ctx.beginPath();
        ctx.rect(cropRect.x, cropRect.y, cropRect.w, cropRect.h);
        ctx.clip();
        ctx.drawImage(cropImg, 0, 0, canvas.width, canvas.height);
        ctx.restore();

        ctx.strokeStyle = '#64ffda';
        ctx.lineWidth = 2;
        ctx.strokeRect(cropRect.x, cropRect.y, cropRect.w, cropRect.h);
      }
    }

    // Certs CRUD
    function renderCerts() {
      if (!Array.isArray(certs) || certs.length === 0) certs = defaultCerts;
      const list = $('cert-list');
      if (!list) return;
      list.innerHTML = '';
      certs.forEach(c => {
        const issuer = c.org || c.issuer || 'Issuer';
        const statusDate = c.date || c.status || '';
        list.innerHTML += `
          <div class="card" style="background:var(--bg-card); border:1px solid var(--border); border-radius:var(--radius-sm); padding:1rem; margin-bottom:0.75rem;">
            <h4 style="color:var(--text); font-weight:700; margin-bottom:0.2rem;">${c.title}</h4>
            <p class="text-muted" style="font-size:0.8rem; color:var(--text-muted);">${issuer} ${statusDate ? '| ' + statusDate : ''}</p>
            ${(c.image || c.img) ? `<img src="${c.image || c.img}" style="max-width:100%; max-height:120px; object-fit:contain; margin-top:0.5rem; border-radius:6px; border:1px solid var(--border);">` : ''}
            <div class="card-actions" style="display:flex; gap:0.5rem; margin-top:0.75rem;">
              <button class="btn btn-primary" onclick="editCert('${c.id}')" style="padding:0.3rem 0.8rem; font-size:0.8rem;">Edit</button>
              <button class="btn btn-danger" onclick="deleteCert('${c.id}')" style="padding:0.3rem 0.8rem; font-size:0.8rem;">Delete</button>
            </div>
          </div>
        `;
      });
      setStorage('custom_certs', certs);
      loadAnalytics();
    }
    
    $('cert-form').addEventListener('submit', e => {
      e.preventDefault();
      const submitBtn = e.target.querySelector('button[type="submit"]');
      const origText = submitBtn ? submitBtn.innerText : 'Save Certification';
      if (submitBtn) submitBtn.innerHTML = '⚡ Encrypting & Syncing Certificate...';

      setTimeout(() => {
        const id = $('cert-id').value || generateId();
        const cert = {
          id,
          title: $('cert-title').value,
          org: $('cert-org').value,
          date: $('cert-date').value,
          desc: $('cert-desc').value,
          image: $('cert-image').value
        };
        
        const idx = certs.findIndex(c => String(c.id) === String(id));
        if(idx > -1) certs[idx] = cert;
        else certs.push(cert);
        
        renderCerts();
        if (window.PortfolioAPI) PortfolioAPI.saveCert(cert);
        e.target.reset();
        $('cert-id').value = '';
        $('crop-container').style.display = 'none';
        $('crop-preview').style.display = 'none';
        $('cert-form-title').innerText = 'Add Certification';
        $('cert-cancel').style.display = 'none';

        if (submitBtn) {
          submitBtn.innerText = '✓ Certification Saved!';
          setTimeout(() => { submitBtn.innerText = origText; }, 2000);
        }

        showToast('🏆 Certification Saved & Live on Portfolio!');
      }, 500);
    });

    window.editCert = id => {
      const c = certs.find(x => String(x.id) === String(id));
      if(!c) return;
      $('cert-id').value = c.id;
      $('cert-title').value = c.title || '';
      $('cert-org').value = c.org || c.issuer || '';
      $('cert-date').value = c.date || c.status || '';
      $('cert-desc').value = c.desc || c.description || '';
      const imgVal = c.image || c.img || '';
      $('cert-image').value = imgVal;
      if (imgVal) {
        if (imgVal.indexOf('data:application/pdf') === 0 || imgVal.toLowerCase().endsWith('.pdf')) {
          if ($('pdf-preview')) $('pdf-preview').style.display = 'block';
          $('crop-preview').style.display = 'none';
        } else {
          if ($('pdf-preview')) $('pdf-preview').style.display = 'none';
          $('crop-preview').src = imgVal;
          $('crop-preview').style.display = 'block';
        }
      } else {
        if ($('pdf-preview')) $('pdf-preview').style.display = 'none';
        $('crop-preview').style.display = 'none';
      }
      $('cert-form-title').innerText = 'Edit Certification';
      $('cert-cancel').style.display = 'inline-block';
      window.scrollTo(0, $('certs').offsetTop);
    };

    window.deleteCert = id => {
      if(!confirm('Delete this certification?')) return;
      certs = certs.filter(c => String(c.id) !== String(id));
      renderCerts();
      showToast('Deleted!');
    };

    window.restoreCerts = () => {
      certs = JSON.parse(JSON.stringify(defaultCerts));
      setStorage('custom_certs', certs);
      renderCerts();
      showToast('Default certifications restored!');
    };

    $('cert-cancel').addEventListener('click', () => {
      $('cert-form').reset();
      $('cert-id').value = '';
      $('cert-form-title').innerText = 'Add Certification';
      $('cert-cancel').style.display = 'none';
      $('crop-container').style.display = 'none';
      $('crop-preview').style.display = 'none';
    });

    // Projects CRUD
    function renderProjects() {
      if (!Array.isArray(projects) || projects.length === 0) projects = defaultProjects;
      const list = $('proj-list');
      if (!list) return;
      list.innerHTML = '';
      projects.forEach(p => {
        list.innerHTML += `
          <div class="card" style="background:var(--bg-card); border:1px solid var(--border); border-radius:var(--radius-sm); padding:1rem; margin-bottom:0.75rem;">
            <h4 style="color:var(--text); font-weight:700; margin-bottom:0.2rem;">${p.title}</h4>
            <p class="text-muted" style="font-size:0.8rem; color:var(--text-muted);">${p.category} | ${p.tags}</p>
            <div style="margin: 0.5rem 0; display: flex; align-items: center; gap: 0.5rem;">
              <span class="proj-status-badge status-${p.status || 'ongoing'}">${p.status || 'ongoing'}</span>
              <span class="proj-progress-pct">${p.progress !== undefined ? p.progress : 0}%</span>
            </div>
            <div class="card-actions" style="display:flex; gap:0.5rem; margin-top:0.75rem;">
              <button class="btn btn-primary" onclick="editProj('${p.id}')" style="padding:0.3rem 0.8rem; font-size:0.8rem;">Edit</button>
              <button class="btn btn-danger" onclick="deleteProj('${p.id}')" style="padding:0.3rem 0.8rem; font-size:0.8rem;">Delete</button>
            </div>
          </div>
        `;
      });
      setStorage('custom_projects', projects);
      loadAnalytics();
    }
    
    if ($('proj-file')) {
      $('proj-file').addEventListener('change', e => {
        const file = e.target.files[0];
        if (!file) return;
        const safeFileName = file.name.replace(/[^a-zA-Z0-9_.-]/g, '_');
        const reader = new FileReader();
        reader.onload = ev => {
          $('proj-image').value = safeFileName;

          // Auto-download for repo
          const a = document.createElement('a');
          a.href = ev.target.result;
          a.download = safeFileName;
          a.click();

          showToast('⚡ Method 1 Automated! Downloaded ' + safeFileName + ' — copy to G:\\Portfolio\\!');
        };
        reader.readAsDataURL(file);
      });
    }
    
    $('proj-form').addEventListener('submit', e => {
      e.preventDefault();
      const submitBtn = e.target.querySelector('button[type="submit"]');
      const origText = submitBtn ? submitBtn.innerText : 'Save Project';
      if (submitBtn) submitBtn.innerHTML = '⚡ Compiling & Syncing Project...';

      setTimeout(() => {
        const id = $('proj-id').value || generateId();
        const proj = {
          id,
          title: $('proj-title').value,
          category: $('proj-cat').value,
          image: $('proj-image').value,
          gallery: $('proj-gallery') ? $('proj-gallery').value : '',
          tags: $('proj-tags').value,
          summary: $('proj-summary').value,
          details: $('proj-details').value,
          status: $('projStatus').value,
          progress: parseInt($('projProgress').value) || 0,
          demoUrl: $('projDemo').value,
          repoUrl: $('projRepo').value
        };
        
        const idx = projects.findIndex(x => String(x.id) === String(id));
        if(idx > -1) projects[idx] = proj;
        else projects.push(proj);
        
        renderProjects();
        if (window.PortfolioAPI) PortfolioAPI.saveProject(proj);
        e.target.reset();
        $('proj-id').value = '';
        $('proj-form-title').innerText = 'Add Project';
        $('proj-cancel').style.display = 'none';

        if (submitBtn) {
          submitBtn.innerText = '✓ Project Saved!';
          setTimeout(() => { submitBtn.innerText = origText; }, 2000);
        }

        showToast('💻 Project Showcase Saved & Live on Portfolio!');
      }, 500);
    });

    window.editProj = id => {
      const p = projects.find(x => String(x.id) === String(id));
      if(!p) return;
      $('proj-id').value = p.id;
      $('proj-title').value = p.title || '';
      if ($('proj-cat')) $('proj-cat').value = p.category || 'cloud';
      $('proj-image').value = p.image || p.img || '';
      if ($('proj-gallery')) $('proj-gallery').value = p.gallery || p.image || '';
      $('proj-tags').value = p.tags || '';
      $('proj-summary').value = p.summary || '';
      $('proj-details').value = p.details || p.summary || '';
      $('projStatus').value = p.status || 'ongoing';
      $('projProgress').value = p.progress !== undefined ? p.progress : 65;
      if ($('projProgressVal')) $('projProgressVal').innerText = $('projProgress').value;
      $('projDemo').value = p.demoUrl || '';
      $('projRepo').value = p.repoUrl || '';
      
      $('proj-form-title').innerText = 'Edit Project';
      $('proj-cancel').style.display = 'inline-block';
      window.scrollTo(0, $('projects').offsetTop);
    };

    window.deleteProj = id => {
      if(!confirm('Delete this project?')) return;
      projects = projects.filter(x => String(x.id) !== String(id));
      renderProjects();
      showToast('Deleted!');
    };

    window.restoreProjects = () => {
      projects = JSON.parse(JSON.stringify(defaultProjects));
      setStorage('custom_projects', projects);
      renderProjects();
      showToast('Default projects restored!');
    };

    $('proj-cancel').addEventListener('click', () => {
      $('proj-form').reset();
      $('proj-id').value = '';
      if ($('projProgressVal')) $('projProgressVal').innerText = '65';
      $('proj-form-title').innerText = 'Add Project';
      $('proj-cancel').style.display = 'none';
    });


    // Skills CRUD
    function renderSkills() {
      const tb = $('skill-tbody');
      tb.innerHTML = '';
      skills.forEach((s, i) => {
        tb.innerHTML += `
          <tr>
            <td>
              <button onclick="moveSkill(${i}, -1)">↑</button>
              <button onclick="moveSkill(${i}, 1)">↓</button>
            </td>
            <td>${s.icon || ''}</td>
            <td>${s.name}</td>
            <td>${s.category || ''}</td>
            <td>${s.pct}%</td>
            <td>
              <button class="btn btn-primary" onclick="editSkill(${i})" style="padding:0.2rem 0.5rem;">Edit</button>
              <button class="btn btn-danger" onclick="deleteSkill(${i})" style="padding:0.2rem 0.5rem;">Del</button>
            </td>
          </tr>
        `;
      });
      setStorage('custom_skills', skills);
      if (window.PortfolioAPI) PortfolioAPI.saveSkills(skills);
    }

    $('skill-form').addEventListener('submit', e => {
      e.preventDefault();
      const idx = $('skill-index').value;
      const skill = {
        name: $('skill-name').value,
        icon: $('skill-icon').value,
        pct: parseInt($('skill-pct').value),
        category: $('skill-cat').value
      };
      if(idx !== '') skills[idx] = skill;
      else skills.push(skill);
      renderSkills();
      e.target.reset();
      $('skill-index').value = '';
      $('skill-cancel').style.display = 'none';
      showToast('Skill saved!');
    });

    window.editSkill = i => {
      const s = skills[i];
      $('skill-index').value = i;
      $('skill-name').value = s.name;
      $('skill-icon').value = s.icon || '';
      $('skill-pct').value = s.pct;
      $('skill-cat').value = s.category || '';
      $('skill-cancel').style.display = 'inline-block';
    };
    window.deleteSkill = i => {
      if(confirm('Delete skill?')) {
        skills.splice(i, 1);
        renderSkills();
      }
    };
    window.moveSkill = (i, dir) => {
      if(i + dir < 0 || i + dir >= skills.length) return;
      const temp = skills[i];
      skills[i] = skills[i + dir];
      skills[i + dir] = temp;
      renderSkills();
    };
    $('skill-cancel').addEventListener('click', () => {
      $('skill-form').reset();
      $('skill-index').value = '';
      $('skill-cancel').style.display = 'none';
    });
    $('skill-restore').addEventListener('click', () => {
      if(confirm('Restore default skills? All changes will be lost.')) {
        skills = [...defaultSkills];
        renderSkills();
        showToast('Default skills restored!');
      }
    });

    // Blog CRUD
    function renderBlogs() {
      if (!Array.isArray(blogs) || blogs.length === 0) blogs = defaultBlogs;
      const list = $('blog-list');
      if (!list) return;
      list.innerHTML = '';
      blogs.forEach(b => {
        list.innerHTML += `
          <div class="card" style="background:var(--bg-card); border:1px solid var(--border); border-radius:var(--radius-sm); padding:1rem; margin-bottom:0.75rem;">
            <h4 style="color:var(--text); font-weight:700; margin-bottom:0.2rem;">${b.title}</h4>
            <p class="text-muted" style="font-size:0.8rem; color:var(--text-muted);">${b.category || 'Tech'} | ${b.date || ''}</p>
            <div class="card-actions" style="display:flex; gap:0.5rem; margin-top:0.75rem;">
              <button class="btn btn-primary" onclick="editBlog('${b.id}')" style="padding:0.3rem 0.8rem; font-size:0.8rem;">Edit</button>
              <button class="btn btn-danger" onclick="deleteBlog('${b.id}')" style="padding:0.3rem 0.8rem; font-size:0.8rem;">Delete</button>
            </div>
          </div>
        `;
      });
      setStorage('custom_blog_posts', blogs);
      loadAnalytics();
    }
    
    if ($('blog-image-file')) {
      $('blog-image-file').addEventListener('change', e => {
        const file = e.target.files[0];
        if (!file) return;
        const reader = new FileReader();
        reader.onload = ev => {
          $('blog-image').value = ev.target.result;
          showToast('Blog cover image uploaded!');
        };
        reader.readAsDataURL(file);
      });
    }

    if ($('blog-pdf-file')) {
      $('blog-pdf-file').addEventListener('change', e => {
        const file = e.target.files[0];
        if (!file) return;
        const reader = new FileReader();
        reader.onload = ev => {
          $('blog-pdf').value = ev.target.result;
          if ($('blog-pdf-status')) $('blog-pdf-status').innerText = '📄 Document Attached: ' + file.name;
          showToast('Blog PDF Report / Document attached!');
        };
        reader.readAsDataURL(file);
      });
    }

    $('blog-form').addEventListener('submit', e => {
      e.preventDefault();
      e.stopPropagation();
      const id = $('blog-id').value || generateId();
      const blog = {
        id,
        title: $('blog-title').value,
        category: $('blog-cat').value,
        excerpt: $('blog-excerpt').value,
        body: $('blog-body').value,
        image: $('blog-image') ? $('blog-image').value : '',
        pdf: $('blog-pdf') ? $('blog-pdf').value : '',
        gallery: $('blog-gallery') ? $('blog-gallery').value : '',
        date: new Date().toISOString().split('T')[0]
      };
      const idx = blogs.findIndex(x => String(x.id) === String(id));
      if(idx > -1) blogs[idx] = blog;
      else blogs.push(blog);
      renderBlogs();
      setStorage('custom_blog_posts', blogs);
      if (window.PortfolioAPI && typeof PortfolioAPI.syncGlobalCMSData === 'function') {
        PortfolioAPI.syncGlobalCMSData();
      }
      e.target.reset();
      $('blog-id').value = '';
      $('blog-form-title').innerText = 'Write Post';
      $('blog-cancel').style.display = 'none';
      if ($('blog-pdf-status')) $('blog-pdf-status').innerText = '';
      showToast(' Post published & synced live!');
      return false;
    });

    window.editBlog = id => {
      const b = blogs.find(x => String(x.id) === String(id));
      if(!b) return;
      $('blog-id').value = b.id;
      $('blog-title').value = b.title || '';
      $('blog-cat').value = b.category || b.cat || '';
      $('blog-excerpt').value = b.excerpt || '';
      $('blog-body').value = b.body || '';
      if ($('blog-image')) $('blog-image').value = b.image || b.img || '';
      if ($('blog-pdf')) $('blog-pdf').value = b.pdf || b.pdfUrl || '';
      if ($('blog-gallery')) $('blog-gallery').value = b.gallery || '';
      $('blog-form-title').innerText = 'Edit Post';
      $('blog-cancel').style.display = 'inline-block';
      window.scrollTo(0, $('blog').offsetTop);
    };

    window.deleteBlog = id => {
      if(!confirm('Delete this blog post?')) return;
      blogs = blogs.filter(b => String(b.id) !== String(id));
      renderBlogs();
      setStorage('custom_blog_posts', blogs);
      if (window.PortfolioAPI && typeof PortfolioAPI.syncGlobalCMSData === 'function') {
        PortfolioAPI.syncGlobalCMSData();
      }
      showToast('Deleted!');
    };

    $('blog-cancel').addEventListener('click', () => {
      $('blog-form').reset();
      $('blog-id').value = '';
      $('blog-form-title').innerText = 'Write Post';
      $('blog-cancel').style.display = 'none';
    });

    if ($('set-resume-file')) {
      $('set-resume-file').addEventListener('change', e => {
        const file = e.target.files[0];
        if (!file) return;
        const reader = new FileReader();
        reader.onload = ev => {
          localStorage.setItem('custom_resume_data', ev.target.result);
          localStorage.setItem('custom_resume', file.name);
          if ($('set-resume')) $('set-resume').value = file.name;
          if ($('resume-status')) $('resume-status').innerText = '📄 Resume Uploaded: ' + file.name + ' (Live on site!)';
          showToast('New Resume uploaded! Live on site download button.');
        };
        reader.readAsDataURL(file);
      });
    }

    // Settings
    function loadSettings() {
      $('set-email').value = localStorage.getItem('custom_email') || '';
      $('set-phone').value = localStorage.getItem('custom_phone') || '';
      $('set-github').value = localStorage.getItem('custom_github') || '';
      $('set-linkedin').value = localStorage.getItem('custom_linkedin') || '';
      if ($('set-resume')) $('set-resume').value = localStorage.getItem('custom_resume') || '';

      if ($('set-admin-user')) $('set-admin-user').value = localStorage.getItem('custom_admin_user') || DEFAULT_USER;
      if ($('set-admin-pass')) $('set-admin-pass').value = '';

      const resData = localStorage.getItem('custom_resume_data');
      if (resData && $('resume-status')) $('resume-status').innerText = '📄 Resume Document Attached & Saved';

      const keys = ['avail', 'stats', 'about', 'ticker', 'experience', 'projects', 'skills', 'certifications', 'events', 'achievements', 'recs', 'github', 'contact'];
      keys.forEach(k => {
        const storageKey = (k === 'recs') ? 'hide_recommendations' : ('hide_' + k);
        const el = $('toggle-' + k);
        if (el) {
          // Default recs is hidden unless enabled; others default to shown
          if (k === 'recs') {
            el.checked = localStorage.getItem(storageKey) === 'false';
          } else {
            el.checked = localStorage.getItem(storageKey) !== 'true';
          }
        }
      });
    }

    $('settings-form').addEventListener('submit', e => {
      e.preventDefault();
      e.stopPropagation();

      const btn = $('settings-submit-btn') || e.target.querySelector('button[type="submit"]');
      const origText = btn ? btn.innerText : 'Save Settings & Security Credentials';
      if (btn) btn.innerHTML = '⚡ Saving Settings & Credentials...';

      setTimeout(() => {
        localStorage.setItem('custom_email', $('set-email').value);
        localStorage.setItem('custom_phone', $('set-phone').value);
        localStorage.setItem('custom_github', $('set-github').value);
        localStorage.setItem('custom_linkedin', $('set-linkedin').value);

        if ($('set-admin-user') && $('set-admin-user').value.trim()) {
          localStorage.setItem('custom_admin_user', $('set-admin-user').value.trim());
        }
        if ($('set-admin-pass') && $('set-admin-pass').value.trim()) {
          localStorage.setItem('custom_admin_pass', $('set-admin-pass').value.trim());
        }

        const keys = ['avail', 'stats', 'about', 'ticker', 'experience', 'projects', 'skills', 'certifications', 'events', 'achievements', 'recs', 'github', 'contact'];
        keys.forEach(k => {
          const storageKey = (k === 'recs') ? 'hide_recommendations' : ('hide_' + k);
          const el = $('toggle-' + k);
          if (el) {
            localStorage.setItem(storageKey, el.checked ? 'false' : 'true');
          }
        });

        if (window.PortfolioAPI && typeof PortfolioAPI.syncGlobalCMSData === 'function') {
          PortfolioAPI.syncGlobalCMSData();
        }

        if (btn) {
          btn.innerText = '✓ Settings Saved Successfully!';
          setTimeout(() => { btn.innerText = origText; }, 2500);
        }

        const alertEl = $('settings-alert');
        if (alertEl) {
          alertEl.className = 'status-alert success';
          alertEl.innerHTML = '✅ All site settings, section visibilities, resume, and security credentials saved live!';
          alertEl.style.display = 'flex';
          setTimeout(() => { alertEl.style.display = 'none'; }, 6000);
        }

        showToast('⚙️ Settings & Security Credentials saved live!');
      }, 400);

      return false;
    });

    // Database Backup
    function loadDbPreview() {
      const data = {};
      for(let i=0; i<localStorage.length; i++) {
        const k = localStorage.key(i);
        if(k.startsWith('custom_') || k.startsWith('site_')) {
          data[k] = getStorage(k, localStorage.getItem(k));
        }
      }
      $('db-preview').value = JSON.stringify(data, null, 2);
    }
    
    $('db-export').addEventListener('click', () => {
      const data = $('db-preview').value;
      const blob = new Blob([data], { type: 'application/json' });
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = `portfolio_backup_${new Date().getTime()}.json`;
      a.click();
      URL.revokeObjectURL(url);
    });

    $('db-import').addEventListener('change', e => {
      const file = e.target.files[0];
      if(!file) return;
      const reader = new FileReader();
      reader.onload = ev => {
        try {
          const data = JSON.parse(ev.target.result);
          Object.keys(data).forEach(k => {
            setStorage(k, data[k]);
          });
          initAll();
          showToast('Data imported successfully!');
        } catch(err) {
          alert('Invalid JSON file');
        }
      };
      reader.readAsText(file);
    });

    $('db-reset').addEventListener('click', () => {
      if(confirm('DANGER! This will delete all custom data. Are you sure?')) {
        const keys = [];
        for(let i=0; i<localStorage.length; i++) {
          if(localStorage.key(i).startsWith('custom_') || localStorage.key(i).startsWith('site_')) {
            keys.push(localStorage.key(i));
          }
        }
        keys.forEach(k => localStorage.removeItem(k));
        location.reload();
      }
    });

    /* ══ ACCESS LOGS & IP TRACKER LOGIC WITH CROSS-DEVICE CLOUD SYNC ══ */
    async function captureCurrentAccessLog() {
      const now = new Date().toLocaleString();
      const ua = navigator.userAgent;
      let browser = 'Unknown Browser';
      if (ua.indexOf('Chrome') > -1) browser = 'Chrome';
      else if (ua.indexOf('Safari') > -1) browser = 'Safari';
      else if (ua.indexOf('Firefox') > -1) browser = 'Firefox';
      else if (ua.indexOf('Edg') > -1) browser = 'Edge';

      const platform = navigator.platform || 'Windows';
      const screenRes = window.screen ? `${window.screen.width}x${window.screen.height}` : 'N/A';

      try {
        const res = await fetch('https://ipapi.co/json/');
        if (res.ok) {
          const data = await res.json();
          const newLog = {
            id: generateId(),
            timestamp: now,
            ip: data.ip || '127.0.0.1',
            location: `${data.city || 'Coimbatore'}, ${data.region || 'TN'}, ${data.country_name || 'India'} (${data.org || 'ISP'})`,
            device: `${browser} on ${platform} (${screenRes})`,
            status: 'Authorized Admin'
          };
          if (window.PortfolioAPI && window.PortfolioAPI.syncAccessLog) {
            await window.PortfolioAPI.syncAccessLog(newLog);
          }
          renderAccessLogs();
          return;
        }
      } catch (e) {}

      try {
        const res = await fetch('https://api.ipify.org?format=json');
        const data = await res.json();
        const fallbackLog = {
          id: generateId(),
          timestamp: now,
          ip: data.ip || '127.0.0.1',
          location: 'Coimbatore, TN / India',
          device: `${browser} on ${platform} (${screenRes})`,
          status: 'Authorized Admin'
        };
        if (window.PortfolioAPI && window.PortfolioAPI.syncAccessLog) {
          await window.PortfolioAPI.syncAccessLog(fallbackLog);
        }
        renderAccessLogs();
      } catch (err) {}
    }

    async function renderAccessLogs() {
      let logs = [];
      if (window.PortfolioAPI && window.PortfolioAPI.fetchGlobalAccessLogs) {
        logs = await window.PortfolioAPI.fetchGlobalAccessLogs();
      } else {
        logs = getStorage('admin_access_logs', []);
      }

      if (!Array.isArray(logs) || logs.length === 0) {
        logs = [{
          id: 'log_1',
          timestamp: new Date().toLocaleString(),
          ip: 'Active Session (You)',
          location: 'Coimbatore, India',
          device: navigator.userAgent.split(' ')[0] + ' on ' + (navigator.platform || 'Win32'),
          status: 'Authorized Admin'
        }];
      }

      const tb = $('access-logs-tbody');
      if (!tb) return;
      tb.innerHTML = '';
      logs.forEach(l => {
        const isFailed = l.status && l.status.indexOf('FAILED') > -1;
        const statusBadge = isFailed
          ? `<span style="background:rgba(244,63,94,0.12); color:var(--rose); border:1px solid rgba(244,63,94,0.3); padding:0.15rem 0.55rem; border-radius:12px; font-size:0.72rem; font-weight:600;">${l.status}</span>`
          : `<span style="background:rgba(52,211,153,0.12); color:var(--emerald); border:1px solid rgba(52,211,153,0.3); padding:0.15rem 0.55rem; border-radius:12px; font-size:0.72rem; font-weight:600;">${l.status}</span>`;

        tb.innerHTML += `
          <tr style="border-bottom:1px solid var(--border);">
            <td style="padding:0.75rem; color:var(--text-muted);">${l.timestamp}</td>
            <td style="padding:0.75rem; font-family:'DM Mono',monospace; color:var(--teal); font-weight:600;">${l.ip}</td>
            <td style="padding:0.75rem; color:var(--text);">${l.location}</td>
            <td style="padding:0.75rem; font-size:0.78rem; color:var(--text-muted);">${l.device}</td>
            <td style="padding:0.75rem;">${statusBadge}</td>
          </tr>
        `;
      });
      if ($('log-count-badge')) $('log-count-badge').innerText = `${logs.length} Cross-Device Sessions Logged`;
    }

    /* ══ RESUME PDF & DOCUMENT FILE UPLOADER ══ */
    const profResumeFile = $('prof-resume-file');
    if (profResumeFile) {
      profResumeFile.addEventListener('change', e => {
        const file = e.target.files[0];
        if (!file) return;
        const reader = new FileReader();
        reader.onload = ev => {
          const resUrl = ev.target.result;
          if ($('prof-resume-url')) $('prof-resume-url').value = resUrl;
          if ($('resume-preview-status')) $('resume-preview-status').style.display = 'block';
          if ($('resume-preview-link')) $('resume-preview-link').href = resUrl;
          showToast('📄 Resume document loaded! Click "Save Profile & Resume PDF" to apply site-wide.');
        };
        reader.readAsDataURL(file);
      });
    }

    // Load Resume URL on profile tab open
    const origLoadProfile = typeof loadProfile === 'function' ? loadProfile : null;
    loadProfile = function() {
      if (origLoadProfile) origLoadProfile();
      const savedRes = localStorage.getItem('custom_resume_pdf') || '';
      if (savedRes && $('prof-resume-url')) {
        $('prof-resume-url').value = savedRes;
        if ($('resume-preview-status')) $('resume-preview-status').style.display = 'block';
        if ($('resume-preview-link')) $('resume-preview-link').href = savedRes;
      }
    };

    /* ══ TIMELINE & EXPERIENCE CRUD ══ */
    function renderTimelineList() {
      const container = $('exp-list');
      if (!container) return;
      let items = getStorage('custom_timeline', defaultTimeline);
      container.innerHTML = items.map((t, idx) => `
        <div class="card" style="background:rgba(255,255,255,0.02); border:1px solid var(--border); border-radius:8px; padding:1.2rem;">
          <p style="font-family:'DM Mono',monospace; font-size:0.75rem; color:var(--teal); font-weight:600; margin-bottom:0.2rem;">${t.date}</p>
          <h4 style="margin:0 0 0.3rem; color:#fff; font-size:1rem;">${t.role}</h4>
          <p style="margin:0 0 0.5rem; font-size:0.8rem; color:var(--text-muted);">${t.org}</p>
          <p style="font-size:0.82rem; color:var(--text-dim); line-height:1.5; margin-bottom:1rem;">${t.desc}</p>
          <div style="display:flex; gap:0.5rem;">
            <button type="button" class="btn btn-primary" onclick="editExperience('${t.id}')" style="padding:0.25rem 0.65rem; font-size:0.75rem;">✏️ Edit</button>
            <button type="button" class="btn btn-danger" onclick="deleteExperience('${t.id}')" style="padding:0.25rem 0.65rem; font-size:0.75rem;">🗑️ Delete</button>
          </div>
        </div>
      `).join('');
    }

    window.saveExperience = () => {
      const id = $('exp-id').value || generateId();
      const role = $('exp-role').value;
      const org = $('exp-org').value;
      const date = $('exp-date').value;
      const desc = $('exp-desc').value;

      let items = getStorage('custom_timeline', defaultTimeline);
      const existingIdx = items.findIndex(item => item.id === id);
      const newEntry = { id, role, org, date, desc };

      if (existingIdx > -1) {
        items[existingIdx] = newEntry;
      } else {
        items.unshift(newEntry);
      }

      setStorage('custom_timeline', items);
      if (window.PortfolioAPI && window.PortfolioAPI.syncTimeline) {
        window.PortfolioAPI.syncTimeline(items);
      }
      renderTimelineList();
      clearExpForm();
      showToast('✓ Experience timeline entry saved successfully!');
    };

    window.editExperience = (id) => {
      let items = getStorage('custom_timeline', defaultTimeline);
      const item = items.find(t => t.id === id);
      if (!item) return;
      $('exp-id').value = item.id;
      $('exp-role').value = item.role;
      $('exp-org').value = item.org;
      $('exp-date').value = item.date;
      $('exp-desc').value = item.desc;
      if ($('exp-form-title')) $('exp-form-title').innerText = 'Edit Timeline Entry';
      if ($('exp-cancel')) $('exp-cancel').style.display = 'inline-block';
      window.scrollTo({ top: $('exp-form').offsetTop - 80, behavior: 'smooth' });
    };

    window.deleteExperience = (id) => {
      if (!confirm('Delete this timeline entry?')) return;
      let items = getStorage('custom_timeline', defaultTimeline);
      items = items.filter(t => t.id !== id);
      setStorage('custom_timeline', items);
      if (window.PortfolioAPI && window.PortfolioAPI.syncTimeline) {
        window.PortfolioAPI.syncTimeline(items);
      }
      renderTimelineList();
      showToast('🗑️ Timeline entry deleted.');
    };

    window.restoreTimeline = () => {
      if (!confirm('Restore default experience timeline entries?')) return;
      setStorage('custom_timeline', defaultTimeline);
      renderTimelineList();
      showToast('↻ Default timeline entries restored!');
    };

    window.clearExpForm = () => {
      $('exp-id').value = '';
      $('exp-role').value = '';
      $('exp-org').value = '';
      $('exp-date').value = '';
      $('exp-desc').value = '';
      if ($('exp-form-title')) $('exp-form-title').innerText = 'Add Timeline Entry';
      if ($('exp-cancel')) $('exp-cancel').style.display = 'none';
    };

    /* ══ TOOLING TICKER & ECOSYSTEM CRUD ══ */
    function renderTickerList() {
      const container = $('ticker-list');
      if (!container) return;
      let items = getStorage('custom_ticker', defaultTicker);
      container.innerHTML = items.map((t, idx) => `
        <div class="card" style="background:rgba(255,255,255,0.02); border:1px solid var(--border); border-radius:8px; padding:1rem; display:flex; align-items:center; justify-content:space-between;">
          <div style="display:flex; align-items:center; gap:0.75rem;">
            <span style="font-size:1.4rem; font-weight:700; color:var(--teal); font-family:'DM Mono',monospace;">${t.icon && t.icon.length > 2 ? '🖼️' : (t.icon || '⚡')}</span>
            <div><h4 style="margin:0; color:#fff; font-size:0.95rem;">${t.name}</h4><p style="margin:0; font-size:0.75rem; color:var(--text-muted); font-family:'DM Mono',monospace;">Icon: ${t.icon}</p></div>
          </div>
          <div style="display:flex; gap:0.4rem;">
            <button type="button" class="btn btn-primary" onclick="editTickerItem('${t.id}')" style="padding:0.25rem 0.55rem; font-size:0.75rem;">✏️ Edit</button>
            <button type="button" class="btn btn-danger" onclick="deleteTickerItem('${t.id}')" style="padding:0.25rem 0.55rem; font-size:0.75rem;">🗑️ Delete</button>
          </div>
        </div>
      `).join('');
    }

    window.saveTickerItem = () => {
      const id = $('ticker-id').value || generateId();
      const name = $('ticker-name').value;
      const icon = $('ticker-icon').value;

      let items = getStorage('custom_ticker', defaultTicker);
      const existingIdx = items.findIndex(item => item.id === id);
      const newEntry = { id, name, icon };

      if (existingIdx > -1) {
        items[existingIdx] = newEntry;
      } else {
        items.push(newEntry);
      }

      setStorage('custom_ticker', items);
      if (window.PortfolioAPI && window.PortfolioAPI.syncTicker) {
        window.PortfolioAPI.syncTicker(items);
      }
      renderTickerList();
      clearTickerForm();
      showToast('✓ Tooling ticker item saved successfully!');
    };

    window.editTickerItem = (id) => {
      let items = getStorage('custom_ticker', defaultTicker);
      const item = items.find(t => t.id === id);
      if (!item) return;
      $('ticker-id').value = item.id;
      $('ticker-name').value = item.name;
      $('ticker-icon').value = item.icon;
      if ($('ticker-form-title')) $('ticker-form-title').innerText = 'Edit Ticker Item';
      if ($('ticker-cancel')) $('ticker-cancel').style.display = 'inline-block';
      window.scrollTo({ top: $('ticker-form').offsetTop - 80, behavior: 'smooth' });
    };

    window.deleteTickerItem = (id) => {
      if (!confirm('Delete this ticker item?')) return;
      let items = getStorage('custom_ticker', defaultTicker);
      items = items.filter(t => t.id !== id);
      setStorage('custom_ticker', items);
      if (window.PortfolioAPI && window.PortfolioAPI.syncTicker) {
        window.PortfolioAPI.syncTicker(items);
      }
      renderTickerList();
      showToast('🗑️ Ticker item deleted.');
    };

    window.restoreTicker = () => {
      if (!confirm('Restore default tooling ticker items?')) return;
      setStorage('custom_ticker', defaultTicker);
      renderTickerList();
      showToast('↻ Default ticker items restored!');
    };

    window.clearTickerForm = () => {
      $('ticker-id').value = '';
      $('ticker-name').value = '';
      $('ticker-icon').value = '';
      if ($('ticker-form-title')) $('ticker-form-title').innerText = 'Add Tooling Ticker Item';
      if ($('ticker-cancel')) $('ticker-cancel').style.display = 'none';
    };

    /* ══ EVENTS & LEADERSHIP SHOWCASE CRUD ══ */
    function renderEventsList() {
      const container = $('events-list');
      if (!container) return;
      let items = getStorage('custom_events', defaultEvents);
      container.innerHTML = items.map((e, idx) => `
        <div class="card" style="background:rgba(255,255,255,0.02); border:1px solid var(--border); border-radius:8px; padding:1.2rem;">
          <div style="display:flex; align-items:center; gap:0.75rem; margin-bottom:0.4rem;">
            <span style="font-size:1.4rem;">${e.icon || '🎖️'}</span>
            <div><h4 style="margin:0; color:#fff; font-size:1rem;">${e.title}</h4><p style="margin:0; font-size:0.78rem; color:var(--teal);">${e.location || e.org}</p></div>
          </div>
          <p style="font-size:0.82rem; color:var(--text-dim); line-height:1.5; margin-bottom:1rem;">${e.desc}</p>
          <div style="display:flex; gap:0.5rem;">
            <button type="button" class="btn btn-primary" onclick="editEventItem('${e.id}')" style="padding:0.25rem 0.65rem; font-size:0.75rem;">✏️ Edit</button>
            <button type="button" class="btn btn-danger" onclick="deleteEventItem('${e.id}')" style="padding:0.25rem 0.65rem; font-size:0.75rem;">🗑️ Delete</button>
          </div>
        </div>
      `).join('');
    }

    window.saveEventItem = () => {
      const id = $('events-id').value || generateId();
      const title = $('events-title').value;
      const org = $('events-org').value;
      const location = $('events-location').value;
      const desc = $('events-desc').value;
      const icon = $('events-icon').value || '🎖️';
      const color = $('events-color').value || 'gold';

      let items = getStorage('custom_events', defaultEvents);
      const existingIdx = items.findIndex(item => item.id === id);
      const newEntry = { id, title, org, location, desc, icon, color };

      if (existingIdx > -1) {
        items[existingIdx] = newEntry;
      } else {
        items.unshift(newEntry);
      }

      setStorage('custom_events', items);
      if (window.PortfolioAPI && window.PortfolioAPI.syncEvents) {
        window.PortfolioAPI.syncEvents(items);
      }
      renderEventsList();
      clearEventsForm();
      showToast('✓ Event / Award item saved successfully!');
    };

    window.editEventItem = (id) => {
      let items = getStorage('custom_events', defaultEvents);
      const item = items.find(e => e.id === id);
      if (!item) return;
      $('events-id').value = item.id;
      $('events-title').value = item.title;
      $('events-org').value = item.org;
      $('events-location').value = item.location;
      $('events-desc').value = item.desc;
      $('events-icon').value = item.icon;
      $('events-color').value = item.color || 'gold';
      if ($('events-form-title')) $('events-form-title').innerText = 'Edit Event / Award Item';
      if ($('events-cancel')) $('events-cancel').style.display = 'inline-block';
      window.scrollTo({ top: $('events-form').offsetTop - 80, behavior: 'smooth' });
    };

    window.deleteEventItem = (id) => {
      if (!confirm('Delete this event item?')) return;
      let items = getStorage('custom_events', defaultEvents);
      items = items.filter(e => e.id !== id);
      setStorage('custom_events', items);
      if (window.PortfolioAPI && window.PortfolioAPI.syncEvents) {
        window.PortfolioAPI.syncEvents(items);
      }
      renderEventsList();
      showToast('🗑️ Event item deleted.');
    };

    window.restoreEvents = () => {
      if (!confirm('Restore default events & leadership items?')) return;
      setStorage('custom_events', defaultEvents);
      renderEventsList();
      showToast('↻ Default event items restored!');
    };

    window.clearEventsForm = () => {
      $('events-id').value = '';
      $('events-title').value = '';
      $('events-org').value = '';
      $('events-location').value = '';
      $('events-desc').value = '';
      $('events-icon').value = '';
      $('events-color').value = 'gold';
      if ($('events-form-title')) $('events-form-title').innerText = 'Add Event / Award Item';
      if ($('events-cancel')) $('events-cancel').style.display = 'none';
    };

    /* ══ SECURE AUTHENTICATION GATE LOGIC ══ */
    async function hashSHA256(str) {
      const msgBuffer = new TextEncoder().encode(str);
      const hashBuffer = await crypto.subtle.digest('SHA-256', msgBuffer);
      const hashArray = Array.from(new Uint8Array(hashBuffer));
      return hashArray.map(b => b.toString(16).padStart(2, '0')).join('');
    }

    let failedAttempts = parseInt(localStorage.getItem('admin_failed_attempts') || '0');
    let lockoutUntil = parseInt(localStorage.getItem('admin_lockout_until') || '0');

    function checkAuthSession() {
      const now = Date.now();
      if (lockoutUntil > now) {
        showLockoutScreen(Math.ceil((lockoutUntil - now) / 1000));
        return false;
      }
      const sessionToken = sessionStorage.getItem('admin_session') || localStorage.getItem('admin_session');
      if (sessionToken === 'authenticated_admin_2026' || sessionToken) {
        hideLoginModal();
        return true;
      } else {
        showLoginModal();
        return false;
      }
    }

    function showLoginModal() {
      const modal = $('loginModal');
      const container = document.querySelector('.admin-container');
      if (modal) modal.style.display = 'flex';
      if (container) container.style.display = 'none';
      if ($('nav-logout-btn')) $('nav-logout-btn').style.display = 'none';
    }

    function hideLoginModal() {
      const modal = $('loginModal');
      const container = document.querySelector('.admin-container');
      if (modal) modal.style.display = 'none';
      if (container) container.style.display = 'flex';
      if ($('nav-logout-btn')) $('nav-logout-btn').style.display = 'inline-block';
    }

    function showLockoutScreen(secsLeft) {
      showLoginModal();
      const msg = $('login-lockout-msg');
      if (msg) {
        msg.style.display = 'block';
        msg.innerHTML = `⚠️ Account Locked due to 5 failed attempts! Please wait <strong>${secsLeft} seconds</strong>.`;
      }
      if ($('login-submit-btn')) $('login-submit-btn').disabled = true;
    }

    function captureFailedSecurityAttempt(attemptedUser) {
      let logs = getStorage('admin_access_logs', []);
      const now = new Date().toLocaleString();
      const ua = navigator.userAgent;
      let browser = 'Unknown Browser';
      if (ua.indexOf('Chrome') > -1) browser = 'Chrome';
      else if (ua.indexOf('Safari') > -1) browser = 'Safari';

      fetch('https://api.ipify.org?format=json')
        .then(res => res.json())
        .then(data => {
          const ip = data.ip || '127.0.0.1';
          const failedLog = {
            id: generateId(),
            timestamp: now,
            ip: ip,
            location: 'Unauthorized IP Attempt',
            device: `${browser} (Attempted user: ${attemptedUser})`,
            status: '⛔ FAILED LOGIN ATTEMPT'
          };
          if (!Array.isArray(logs)) logs = [];
          logs.unshift(failedLog);
          setStorage('admin_access_logs', logs);
          renderAccessLogs();
        })
        .catch(() => {});
    }

    // Init
    function initAll() {
      const isAuth = window.checkAuthSession();
      loadAnalytics();
      loadProfile();
      renderCerts();
      renderProjects();
      renderSkills();
      renderBlogs();
      loadSettings();
      loadDbPreview();
      if (isAuth) captureCurrentAccessLog();
    }
    
    document.addEventListener('DOMContentLoaded', () => {
      initAll();
      const form = $('admin-login-form');
      if (form) form.addEventListener('submit', window.handleAdminLogin);

      const loginBtn = $('login-submit-btn');
      if (loginBtn) loginBtn.addEventListener('click', window.handleAdminLogin);

      const toggleBtn = $('toggle-pass-btn');
      if (toggleBtn) toggleBtn.addEventListener('click', window.togglePasswordVisibility);

      const navLogoutBtn = $('nav-logout-btn');
      if (navLogoutBtn) navLogoutBtn.addEventListener('click', window.adminLogout);

      const sideLogoutBtn = $('sidebar-logout-btn');
      if (sideLogoutBtn) sideLogoutBtn.addEventListener('click', window.adminLogout);
    });
  </script>
  <div id="toast"></div>
  <script src="script.js?v=25.0"></script>
</body>
</html>
